【AI 经济学】你的 AI 助手每搜一次网，背后烧的是谁的钱？——agent 时代的"水电煤"

━━━━━━━━━━━━━━━━━━━━

◆ 起因：一次让我愣住的配置

━━━━━━━━━━━━━━━━━━━━

先说背景。我日常用的是御三家的官方 agent：Claude Code、Codex（OpenAI）、Gemini CLI——它们都自带"联网"开关，开了就能用，从来没让我配过什么搜索 API。所以过去一年我潜意识里以为，"AI 能上网"是一件理所当然的事——大模型自己接 Google，我管它后端怎么转的。

直到前几天试用一个叫 Hermes 的 agent 工具，配的是字节豆包大模型。装完启动，弹出一个对话框："请配置搜索引擎"。

我下意识以为是让我在 Google 和百度之间二选一——选完它就帮我搜。结果点进去，需要填的是 **Firecrawl 的 API key**。一个我之前没听过的服务。

愣了一下：御三家自己搞掂的事，第三方 agent 怎么就要我亲手填一家陌生公司的 key？豆包就豆包，我让它"搜一下 xxx"，它为什么不直接 Google 或者百度？

这个问题往下挖，挖出来的是一整层在过去一年闷声起飞的赛道——**AI agent 时代的"水电煤"**。它们不写一行 prompt，不训一个模型，但 Cursor 每帮你 vibe 一行代码、Claude Code 每搜一次文档、你那个不知名的 RAG 玩具每查一次资料，背后都有一串第三方 API 在按 token 计费转动。

最便宜的一个估值 $3 亿，最贵的一个估值 $22 亿，**最快的那家一年时间从 A 轮到被收购，溢价 98 倍**。

而绝大多数程序员（包括我），是直到配 key 那一刻，才知道这赛道存在的。

━━━━━━━━━━━━━━━━━━━━

◆ 为什么 AI 不能直接用 Google？

━━━━━━━━━━━━━━━━━━━━

我们先解决最朴素的疑惑：豆包/Claude/GPT 这些大模型自己难道不能上网吗？为什么非要一个中间商？

答案分两层：**能不能技术上做到 ≠ 能不能稳定做到**。

直接抓 Google 这件事，到 2026 年已经基本走不通了。原因不是技术不行，是 Google 那头早就武装到牙齿：

- **SERP 强制 JavaScript 渲染**：你不跑浏览器就拿不到结果，类名两个月一变，2024 年的爬虫脚本到 2026 全部失效
- **reCAPTCHA v3 + IP 信誉系统**：根据"行为评分"识别非人类访问，datacenter 段 IP 直接拒绝
- **Cloudflare 默认屏蔽 AI bot**：2026 年起推 Pay-Per-Crawl 模式，**2.5M+ 网站的 robots.txt 里写明拒绝 AI 训练**，79% 的头部新闻站至少屏蔽一个 AI bot

这还只是技术层。法规层更狠：

- **2025 年 8 月，微软关闭了 Bing 公共搜索 API** ——这是给 AI 应用准备的最后一条"官方搜索通道"，说没就没
- **Google 起诉 SerpAPI**（一家专门抓 Google 结果转卖的公司），整个"搜索结果转售"赛道格局被迫重构
- 英国监管机构 2026 年要求 Google 必须允许新闻站"opt-out"——意思是新闻内容能不能被 AI 用、由新闻站自己说了算

💡 翻译成人话：**搜索引擎是给人看的，不是给 agent 看的。** 给人看的网页可以挂广告、收 cookie、装 JS、藏反爬机关；agent 进来这套全是负担。所以诞生了一个新的中间层——专门把"给人看的 web"翻译成"给 agent 吃的 web"。

这就是 Firecrawl、Tavily、Exa 们存在的理由。

━━━━━━━━━━━━━━━━━━━━

◆ 这层赛道的核心玩家

━━━━━━━━━━━━━━━━━━━━

按功能分三家代表，路线不同：

**Tavily：直接给 LLM 上菜的搜索 API**

它不返回十条蓝链让你点。它直接返回**已经按相关性排好序、切好块、清干净广告的纯文本片段**——就是 LLM 喜欢的那种格式。你的 agent 调一次 API，进来的是"可以直接塞 prompt"的内容。

成绩单：2025-08 拿了 Insight Partners 的 A 轮 $20M。**2026-02 被 Nebius 以 $275M 收购，约 98 倍收入倍数**——这是过去一年这层赛道里最快的退出。SaaS 行业 10 倍收入倍数算正常、20 倍算性感，**98 倍说明买方相信它会成为基础设施**。

**Exa（前身 Metaphor）：语义搜索，500B URL 索引**

它的特点是**理解你想搜的东西的"意思"**，不是匹配关键词。你问"那家做 LLM agent 工具被 OpenAI 投资过的小公司"，传统搜索引擎给不出答案，Exa 能给。

成绩单更猛：
- 2025-09 B 轮 $85M @ $700M 估值（Benchmark + Nvidia 领投）
- **2026-05 C 轮 $250M @ $22 亿估值**（a16z 领投）
- ARR $10M，**YoY 增长 1010%**
- 客户名单：Cursor、Cognition、HubSpot、OpenRouter——半个 AI 圈的精品店都在用

**Firecrawl：网页 → Markdown 的转码器**

我配过的那家。它干的事最朴素——给一个 URL，返回一份 LLM 友好的 markdown，过滤广告、规整代码块、保留链接结构。

成绩单：2025-08 拿了 $14.5M A 轮，投资方包括 Nexus、YC、还有 Shopify CEO Tobi。35 万开发者，GitHub 48k 星——**一年时间从 GitHub 项目长成估值过亿的公司**。

────────────────────

类似定位的还有几家，捎带列一下：

- **Jina AI Reader**（r.jina.ai）：免费额度大、用法极简（URL 前面加个前缀就直接转 markdown）。**2025-10 被 Elastic 收购**
- **SerpAPI**：传统 Google/百度结果抓取，被 Google 起诉了，前途不明
- **Brave Search API**：独立索引（不靠 Google），2026-02 砍掉免费版改全量按量计费——这事在开发者圈里炸过锅
- **Perplexity Sonar API**：把它的整套"边搜边答"能力打包卖
- **You.com API**：商业数据不公开

━━━━━━━━━━━━━━━━━━━━

◆ 不止搜索——agent 的整条供应链

━━━━━━━━━━━━━━━━━━━━

把镜头拉远一点。一个真正能干活的 AI agent，需要的不只是"搜资料"——它要执行代码、要操作浏览器、要持久化记忆、要管理多步任务。每一个动作背后都有一家（或几家）公司在收钱。

| 层 | 干什么 | 代表公司 | 公开估值/融资 |
|---|---|---|---|
| 搜索/检索 | 给 agent 喂搜索结果 | Tavily / Exa / Perplexity | $0.3-2.2B |
| 网页转码 | URL → LLM 友好的格式 | Firecrawl / Jina | $16M / 退出 |
| **代码沙箱** | 让 agent 安全跑代码 | **E2B**（$21M A 轮，88% 财富 100 强是客户）/ **Daytona**（$24M A 轮，两个月破 $1M ARR） | $20M+ |
| **浏览器自动化** | 让 agent 操作真实浏览器 | **Browserbase**（B 轮 $40M @ **$3 亿估值**） | $300M |
| **向量数据库** | agent 的长期记忆 | Pinecone / Weaviate / Qdrant / Milvus | $0.5-1B |
| 入场的巨头 | 同上 | Cloudflare、Vercel、Modal、Snowflake、Databricks 2026 年都发了自己的 sandbox | — |

这是一整层**新冒出来的基础设施**。两年前还不存在的赛道，现在 VC 圈管这一片叫 **"picks and shovels for agents"**——卖给淘金者的铲子。

值得专门展开一段的是**代码沙箱**这层。你想过没——Claude Code 让你说"帮我跑一下这段 Python 看输出"，它真在哪里跑？

如果跑在你电脑上，agent 能改你文件、能装库、能改环境变量，安全模型全废了。所以新一代 agent 工具的做法是：**租 E2B 或 Daytona 的一次性沙箱**，给一个隔离的 Firecracker microVM（同款技术 AWS Lambda 在用），跑完销毁。一次几分几秒钱，agent 替你买单（其实是 Anthropic/OpenAI 替你买单，因为他们要从你的订阅费里扣这部分）。

Browserbase 同理——让 agent 能"看见网页、点按钮、填表单"，不是直接接管你的 Chrome，而是租一个云端的无头浏览器实例。**$3 亿估值不是它技术多牛，是它卡在了 agent 通往 web 的唯一通道上**。

━━━━━━━━━━━━━━━━━━━━

◆ 中国侧：一个反常的空白

━━━━━━━━━━━━━━━━━━━━

这层赛道在中国是什么状态？盘点下来很有意思：

**有的部分**：百度千帆把"百度 AI 搜索" API 化了，集成在 AppBuilder 里；字节豆包 1.6 内置工具调用搜索；火山引擎给企业客户出"联网搜索 API"方案。

**没有的部分**：**没有一家独立的、跨厂商中立的 Tavily/Firecrawl 对标公司**。

差别在哪？美国这层全是创业公司，各自打磨自己的产品、卖给所有 agent 厂家；中国这层全是大厂内嵌，**百度的 API 只在百度生态里好用，字节的搜索绑死在豆包模型上**。你要跨厂用，比如想让豆包模型搭配百度搜索，没有顺手的方案。

这就是为什么我用 Hermes + 豆包还得去填海外 Firecrawl 的 key——**中国大厂闭环的中间，留了一个独立中间件的空白**。

这个空白可能不是技术问题，是商业模式问题。美国的 SaaS 文化允许"小公司只做一层、靠收基础设施费活下来"；中国的互联网文化倾向于"大厂全栈、用户白嫖"。你要在国内做个"专门给 AI agent 卖搜索 API"的公司，融资 PPT 上第一个问题就是"百度做了你怎么办"。

但反过来想：**这个空白本身就是机会**。等中国的 agent 生态长出独立工具链，第一家做对的公司很可能就是这条赛道的国产 Tavily。

━━━━━━━━━━━━━━━━━━━━

◆ 这场暗赛跑的本质：AI 把 web 的消费者换了

━━━━━━━━━━━━━━━━━━━━

把这一切串起来，背后的逻辑就一句话：

**Web 的主要消费者，正在从人类换成 AI agent。**

过去三十年，整个互联网是为人优化的——网页要好看、广告要醒目、按钮要好点、推荐要上瘾。所有基础设施（CDN、浏览器、SEO、广告网络）都围绕"如何让人在网页上停留更久"建起来的。

现在多了一种新消费者，它不看广告、不停留、不滚动、不点赞——它只想要**结构化的、可解析的、按相关性排好序的纯文本**。**人类那套基础设施对 agent 是负优化**：JavaScript 渲染浪费它时间、广告挤占它注意力、反爬机制直接拒它进门。

于是必然有一批新基础设施长出来，专门服务这个新消费者。Tavily 是"agent 的 Google"、Firecrawl 是"agent 的浏览器"、Browserbase 是"agent 的 Chrome"、E2B 是"agent 的本地终端"、Pinecone 是"agent 的硬盘"。

每一个角色在人类世界里都已经有巨头，但 agent 的版本要重新来一遍——因为协议不一样、数据格式不一样、安全模型不一样、计费模式不一样。

**这就是"agent 时代的水电煤"的真正含义**：不是说这些公司像水电煤一样不可或缺（它们当然可替代），是说它们正在重建一遍 web 的底层水管——给一个新的、非人类的消费者用。

谁的水管接好了，谁就能卡住未来十年的 agent 流量。Tavily 的 98 倍倍数收购、Exa 的 1010% 增长、Browserbase 的 $3 亿估值，VC 算的就是这本账。

━━━━━━━━━━━━━━━━━━━━

◆ 给程序员的一句实用提醒

━━━━━━━━━━━━━━━━━━━━

最后说点实际的。

你现在用的每一个 AI 工具（Cursor、Claude Code、各种 vibe coding 玩具、各种 RAG demo），背后都在悄悄烧第三方 API 的钱。**这些钱大概率不是你自己掏，是工具厂商（Anthropic、Cursor 等）从订阅费里扣**——但羊毛出在羊身上，订阅费迟早要涨。

哪天你看到 Cursor 涨价、Claude Max 限额、ChatGPT Pro 取消某项福利——别只骂这些大厂"割韭菜"。它们背后是一整条按 token / 按 query / 按 sandbox 秒数计费的供应链。Tavily 涨一次价、Browserbase 提一次 SLA，这些钱不会凭空消失，最终都会从某个地方咬回你的订阅费。

下一次你的 AI 工具弹出"请配置搜索引擎 API key"——你就知道，那个对话框背后，是一家估值几亿美元的公司在跟另一家估值几十亿美元的公司做生意，**而你恰好是这门生意里那个不知情的中转节点**。

━━━━━━━━━━━━━━━━━━━━

💡 **本文涉及的公司与公开数据：**

- **Tavily**：LLM 友好搜索 API，2025-08 A 轮 $20M，**2026-02 被 Nebius 以 $275M 收购（~98 倍收入倍数）**
- **Exa**（前 Metaphor）：语义搜索 + 500B URL 索引，**2026-05 C 轮 $250M @ $22 亿估值**，ARR $10M / YoY 1010%
- **Firecrawl**：网页 → Markdown，2025-08 A 轮 $14.5M，35 万开发者
- **Jina AI Reader**：URL 前缀转 markdown，2025-10 被 Elastic 收购
- **Browserbase**：云端无头浏览器，B 轮 $40M @ $3 亿估值
- **E2B**：agent 代码沙箱，A 轮 $21M，88% 财富 100 强客户
- **Daytona**：A 轮 $24M（2026-02），两个月破 $1M ARR
- **SerpAPI**：传统搜索结果抓取，被 Google 起诉中
- **Brave Search API**：独立索引，2026-02 取消免费档
- **Cloudflare Pay-Per-Crawl** + **微软 2025-08 关停 Bing 公共搜索 API**：传统 web 抓取通道的双重坍塌

━━━━━━━━━━━━━━━━━━━━

**「AI 把 web 的消费者换了一个物种。围绕新物种的整条基础设施，正在被重建一遍。」**

**「Tavily 不是 Google 的竞争对手，是 Google 的'agent 翻译官'。这一层的护城河，是 Google 自己不想做。」**

**「你每一次让 AI'帮我查一下'，背后都有一家估值过亿的公司在按 query 收钱。这门生意里你不是顾客——你是流量。」**

━━━━━━━━━━━━━━━━━━━━

◆ 下一篇预告

━━━━━━━━━━━━━━━━━━━━

如果说搜索/抓取是 agent 的"嘴和眼"——它去 web 上找东西的工具，那 agent 的"记忆"是什么？

答案是 **embedding API + 向量数据库**。这条赛道更隐蔽，因为大多数程序员根本没意识到自己在用——但里面有更狠的故事：MongoDB 用 $2.2 亿吞了 embedding 圈的精品店，Pinecone 的 ARR 一年从 $26.6M 跌到 $14M（长上下文在吃它的小语料市场），**而中国在 embedding 模型上全球领先，却没能长出一家独立的向量数据库公司**。

下一篇：**你的 RAG 玩具背后烧的是哪家的钱？——embedding 与向量数据库的暗战**。

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的严谨 × Gemini 的浪漫
// 2026-06-15
