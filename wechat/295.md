【AI Agent 开发】你拼的那个 messages 数组，模型根本没见过

━━━━━━━━━━━━━━━━━━━━

◆ 开篇：一个不报错的坑

━━━━━━━━━━━━━━━━━━━━

如果你是做 AI Agent 开发的同学，手里大概有这么一段东西：

```
messages = [
    {"role": "system", "content": "你是一个助手"},
    {"role": "user", "content": "你好"},
    {"role": "assistant", "content": "你好！"},
]
```

一轮轮往里 append，发出去，收回来，再 append。这个数组就是你对“上下文”的全部认识。

直到有一天你换了个模型。

假设你之前跑的是 Qwen，现在想换 Llama 试试。你手上有一段自己写的拼接代码——因为要精确控制 token 数、要做缓存、或者只是当初图省事——它把 messages 拼成了这样：

```
<|im_start|>user
你好<|im_end|>
```

这套写法在 Qwen 上跑得好好的。换到 Llama 上，**不报错**。模型照样回你话，只是答得有点怪。训练时那套专用边界没了之后，能出的岔子大概是这几类：分不清哪句是你说的哪句是它说的，把系统提示词当成对话内容念出来，在该停的地方不停。

你去查日志，messages 数组结构完整，字段一个没少，JSON 格式合法。看不出任何毛病。

毛病在于：**以 Llama 3.1 为例，它的词表里没有 `<|im_start|>` 这个特殊 token**。这串字符会被当成普通文本，切成 `<`、`|`、`im`、`_start`、`|`、`>` 等几个 token。你以为自己用了模型训练时的对话边界，它收到的却是一串普通字符片段。

而这件事没有任何一层会报错。JSON 是合法的，tokenizer 能正常工作，模型能正常生成——它只是不再按训练时的格式看到“谁在说话”。

这个坑之所以值得写一篇，是因为它逼着人回答一个平时不会问的问题：**我拼的这个数组，到模型跟前的时候，到底长什么样？**

答案是：不长那样。JSON 只活在 API 那一段，到模型跟前它已经被改了两次形，花括号、引号、字段名一个都不剩。

━━━━━━━━━━━━━━━━━━━━

◆ 第一章：三层递降

━━━━━━━━━━━━━━━━━━━━

从你的代码到模型的输入，中间有两次改形。三层长这样：

| 层 | 长什么样 | 谁负责 |
|---|---|---|
| 你发的请求 | `{"role":"user","content":"你好"}` | 你的代码 / SDK |
| 模板渲染后 | `<\|im_start\|>user\n你好<\|im_end\|>` | chat template |
| 分词之后 | `[151644, 872, 198, 108386, 151645]` | tokenizer |

**JSON 只在第一层。** 第二层之后，花括号没了，引号没了，`role` 和 `content` 这两个字段名也没了——它们从来没打算进模型，它们只是你和服务端之间的信封。

第二层这个转换叫 chat template，是一段 Jinja2 模板，通常存在模型仓库的 `tokenizer_config.json` 里，和模型文件一起发布。你在 transformers 里调 `apply_chat_template()`，跑的就是它。每家的模板都不一样，这也正是开篇那个坑的来源：**模板是跟着模型一起训出来、一起发布的，不是行业协议。**

第三层就是普通分词。渲染出来的那一长串文本，跟任何别的文本一样过 tokenizer，出来一串整数。

分界线也在这儿：常见 GPU 推理里，前面这两步在 CPU 上处理字符串；过了 tokenizer，整数张量才送进加速设备。再往后拿编号去查的 embedding 表，本身是模型权重。所以 tokenizer 的表和 embedding 表是两张不同的表：前者存“字符串对应哪个编号”，通常是几 MB 到几十 MB 的配置文件；后者存“编号对应哪一行向量”，是训出来的权重。一次模型训练期间，两边的编号必须对齐；新增 token 也得同步扩充并训练 embedding。

到这儿为止，“上下文”这个词的意思已经变了三次：在你眼里它是个数组，在模板眼里它是一段字符串，在模型眼里它是一串整数。而马上会看到，到模型内部它还要再变一次。

━━━━━━━━━━━━━━━━━━━━

◆ 第二章：role 不是一路输入，是几个词

━━━━━━━━━━━━━━━━━━━━

写 API 写久了，很容易产生一个印象：角色是个独立的字段。

这个印象是有来历的。`{"role": "user", "content": "你好"}` 里，role 和 content 是 JSON 对象里并列的两个键，SDK 的参数叫 role，文档管它叫 role，报错信息里还是 role。写了几百次之后，很难不觉得它跟内容是两路东西——内容走内容那条线，角色走角色那条线。

到了模型跟前，这两路合成了一路。

上一节那张表的最后一行已经透露了：`<|im_start|>`(151644) 和“你好”(108386) 排在同一个列表里，中间没有第二路输入。它确实是“特殊 token”——意思是它在 tokenizer 里单独占了一个编号，不会被 BPE 切开，生成框架还可能把某些特殊 token 当成停止标记。但**进入 Transformer 主干以后，它同样只是一个编号，去 embedding 表里取一行向量**。

151644 进入模型之后，走的是查表：拿这个编号去 embedding 表里取出对应的那一行向量。而“苹果”这个词进入模型之后，走的也是查表，取的是另一行。同一张表，同一个动作，同样一行浮点数。

而且这不是训练之后才变得一样的——这些标记刚进词表时，表里对应的那一行就是随机初始化的数，跟“苹果”那行毫无关系，编号本身并不天然带着“用户”或“边界”的意义。它后来能起分界作用，靠的是大量按同一格式组织的对话数据把这种功能学进权重（具体怎么学会的，下面就讲）。出厂时它什么都不是。

所以 role 这个字段并没有丢，它变成了 `<|im_start|>user` 这几个 token，一直在序列里立着。丢掉的是**并列**那层关系——它不再是跟 content 平起平坐的另一路输入，而是内容序列里排在最前面的几个词。

这个区别的后果是：`<|im_start|>user` 立在一段话的开头，而**它后面那些字并不各自携带一份 segment id**。模型得通过注意力利用前面的边界 token、普通单词 `user` 和整套序列格式，才知道自己在哪一段。所谓“谁在说话”，不是单独塞在一张角色表里，而是从这条序列的模式里读出来。

顺带说一句，这不是 decoder-only（只有解码器）的物理限制。架构当然可以再加一张角色表，只是 Llama、Qwen 这类主流文本模型没有采用 BERT 式的 token-type embedding；它们选择把角色格式写进同一条序列。

这就带来一个代价。角色标记从“架构里的一路输入”变成了“序列里的几个 token”之后，格式一旦错配就很脆弱—— **拿错 tokenizer 时它能被切碎，不可信内容也能模仿指令口吻，而这两件事都未必报错。**

“被切碎”就是开篇那个坑：拿错模板，`<|im_start|>` 在对方词表里不是一个 token，被切成一串碎片，对话边界没了。

“被内容模仿”更微妙一点，而这篇稿子在写的过程中正好撞上了一个活例子。

这篇稿子是人跟 AI 一起磨出来的，用的工具是 Claude Code（版本 2.1.236）。它偶尔会犯一个 bug：把 AI 上一条回复的一段，套上 `user` 的外壳，当成“用户的新消息”再塞回给模型。下面这张截图里，红框标出来的一整段——包括 `<total_tokens>` 这种本该只在系统层出现的标记——都不是用户打的，是模型自己上一轮的输出被原样贴了回来：

![Claude Code 把 AI 上一轮的输出贴回成 user 消息](assets/295/claude-code-bug-2026-08.png)

对模型来说，这条伪造的 user 消息和一条真的 user 消息，**在 token 层面没有任何区别**：外壳一样、标记一样，因为那层 `<|im_start|>user` 的包装本来就是外面套上去的，不管里面装的是谁的字。模型收到的显式角色证据就是“这是 user”，却没有另一条可信的来源通道能核验包装是否属实；它最多只能从内容反推这段话读起来像不像自己说过的，而这种反推并不可靠。

────────────────────

💡 BERT 那代，段归属编码在每个 token 里

2018 年的 BERT 要处理成对的输入——问答配不配、上下句连不连、两句话是不是蕴含关系。这类任务得让模型清楚知道每个词属于哪一句。

做法是给每个 token 配三份输入，相加之后再进模型（这里用论文里的叫法，源码里三个变量分别是 `word_embeddings`、`token_type_embeddings`、`position_embeddings`）：

```
token embedding     这个词是什么
segment embedding   这个词属于第一句还是第二句
position embedding  这个词在第几个位置
```

中间那份查的是一张只有两行的表（A 句一行、B 句一行，配置项 `type_vocab_size` 就是 2）。 **每个 token 都各带一份**，位置 5 那个词自己就知道自己是第二句的，不用回头看——这个身份编码在它自己的向量里，不是靠上下文推出来的。

为什么默认是两行，不是三行五行？因为原始 BERT 就按两段式做预训练：`[CLS] 句子A [SEP] 句子B [SEP]`。它当年瞄准的常见任务也正好能整理成一段或两段——

```
问答       问题 + 文章 = 2
推理       前提 + 假设 = 2
相似度     句1 + 句2  = 2
情感分类   1 句
```

这不是技术上只能有两行；配置可以改，但新增的行也得重新训练。 **原始 BERT 只学了 A / B 两类，所以官方配置就是 2。**

值得注意的是，这两行首先标的是**段归属**，不是“问题”“文章”“前提”“假设”这些固定语义。同一张 A / B 表会跨任务复用；只是在具体数据格式里，A 段和 B 段可以分别承载不同功能。所以往回看，它已经在给每个 token 直接标注“属于哪一段”，但还不是今天的 system / user 角色。

到了 Llama、Qwen 这类文本 decoder-only 模型，常见做法只保留 token embedding，再用 RoPE（旋转位置编码）处理位置；BERT 那张 token-type embedding 不再使用。角色本身没有消失，它被写进了序列格式。

今天的序列格式也不是从 BERT 那张表演化来的——GPT 式 decoder-only 模型从一开始就把输入排成单序列，是并行的另一支；后来聊天模型沿着这条路把 system、user、assistant、工具调用和工具返回都做成了序列格式。这样扩展格式不必改 embedding 结构，但**模板必须和训练格式匹配**；只在模板里凭空加个新标记，模型并不会自动学会它的含义。（顺带一提，BERT 配套的 NSP 任务后来也被 RoBERTa 的消融削弱了必要性：去掉 NSP 并没有损害下游表现。）

把两种输入形状并排看，区别是**段归属从逐 token 标注，变成了段首的序列格式**：

```
BERT   编码进每个 token，原始模型学了 A / B 两类
现在   立在每段话开头，可扩展格式，但得跟训练配套
```

灵活性是真换来了。工具调用、工具返回这些格式可以在不改主干架构的前提下加入，只是仍要经过配套训练，不能只改模板。

────────────────────

那模型凭什么能分清？凭大量按同一 chat template 排好的训练样本。`<|im_start|>assistant` 反复出现在助手回答之前，久而久之，这个格式与后续文本分布的关联被学进了权重。 **“谁在说话”是训练出来的格式语义，不是独立角色通道保证的事实。**

这个区别在平时看不出来，出事的时候才显形——比如提示词注入（prompt injection）。外层 role token 明明标着 user 或 tool，但不可信内容里的自然语言指令仍和可信指令进入同一套注意力计算。模型学到的优先级不是一道不可绕过的访问控制；模仿系统口吻可能让攻击更像，却不是攻击成立的前提。 **角色边界存在，不等于数据和指令已经硬隔离。**

────────────────────

💡 角色边界标记，一家一个样

Qwen 那句 `<|im_start|>user\n` 是三个 token：`<|im_start|>`(151644) + `user`(872) + `\n`(198)。注意中间那个 872—— **它就是普通英文单词 “user” 的编号** ，跟你在正文里写 “user” 用的是同一个。角色名在这儿是明文文本。

DeepSeek 走了另一条路。它的模板长这样（注意竖线是全角的 ｜）：

`<｜begin▁of▁sentence｜>你是一个助手<｜User｜>你好<｜Assistant｜>你好！<｜end▁of▁sentence｜>`

`<｜User｜>` 是**单独一个 token，编号 128803**，`<｜Assistant｜>` 是 128804。渲染后的字符串里根本不出现 “user” 这个英文单词，角色信息 100% 编码在符号本身。另外注意开头：system 提示词紧跟着句首标记裸拼，**连个角色标记都没有** ——在 DeepSeek 的模板里，所谓“系统角色”就是句子开头的一段话。

（以上是 V3/R1 那一代的 tokenizer。早期 DeepSeek LLM/Coder 用的是 `User:` 明文那套，别外推。）

两家的写法差这么远，是因为**这套边界标记根本没有标准**：用明文还是符号、编几号、system 要不要独立标记，全是各家训练时自己定的，定了就跟训练格式绑在一起。这也正是开篇那个坑的根：你拿 A 家的写法套 B 家，B 家收不到训练时学过的原子边界 token，只能把它当普通字符片段处理。它也许能从字符和语境猜出一点意思，但已经不是那套专用边界了。模板不是行业协议，是每个模型的私人方言。

────────────────────

顺带一个有意思的细节：DeepSeek-R1 里 `<think>` 是 128798、`</think>` 是 128799，而在 V3 的同样两个位置上，蹲着的是 `<｜place▁holder▁no▁798｜>` 和 `<｜place▁holder▁no▁799｜>`——预留的空槽，编号跟 id 一一对齐。V3 从 128000 到 128799 一共有 800 个这样的空槽；R1 保留前 798 个，只把最后两个换成了思维链标记。 **从两份词表的差异看，R1 复用了两个预留槽位。** 这个细节等会儿还要用到。

━━━━━━━━━━━━━━━━━━━━

◆ 第三章：图片是怎么进来的

━━━━━━━━━━━━━━━━━━━━

文本这条路走通了，多模态就顺理成章：图片也得变成同一条序列上的东西，才能跟文字一起被处理。

问题是图片没法查表。词表里没有“这张图”这一行，也不可能有。

Qwen-VL 的做法是**占位加替换**。渲染后的序列里，一张图长这样：

```
<|vision_start|> <|image_pad|> × N <|vision_end|>
```

中间那 N 个 `<|image_pad|>`（编号 151655）就是占位符。它们先跟普通 token 一样完成分词、查 embedding 表，得到 N 行占位向量。然后在进入语言模型主干之前，这 N 行被视觉塔的输出**原地覆盖**掉。

为什么绕这一圈——先造一批假向量、再把它们覆盖掉，而不是直接把图片插进去？因为这么一绕，图片就伪装成了 N 个普通 token：位置编号、注意力范围、显存分配，全都顺着文本那条打磨多年的成熟管线自动算好，不用为图片单独改任何底层逻辑。图片假装成文字混在序列里走完全程，只在最后一步被换成真身——Transformer 主干根本不知道里面掺了图片。“专门给图片开一条通道”听着直接，真做起来才是折腾。

源码里就是一句话（Qwen 团队提交给 transformers 库的 `modeling_qwen2_5_vl.py`，是公开的建模代码，跟着权重一起发在 HuggingFace 上）：

```
inputs_embeds = inputs_embeds.masked_scatter(image_mask, image_embeds)
```

`masked_scatter` 的语义是按 mask 逐位置填充，前提是 **mask 里 True 的个数和填充数据的行数严格相等**。代码在调它之前先把两边的数量点了一遍，对不上就当场抛错，差一个都不行。

于是“如何确保图片和 token 严格对齐”这个问题，答案有点朴素：Processor 和视觉塔共享同一份网格尺寸 `image_grid_thw`，前者据此展开占位符，后者据此产出视觉特征，最后再核对数量。 **不是靠模型猜位置，是两边共用一份尺寸，算错就崩。**

在没有触发最小、最大像素限制时，公式是这样的。先是尺寸吸附——图片被拉到 factor 的整数倍：

```
factor = patch_size × merge_size
h_bar = round(H / factor) × factor
w_bar = round(W / factor) × factor
```

然后数占位符：

```
N = (h_bar × w_bar) / factor²
```

如果图片太大或太小，源码还会按 `max_pixels` / `min_pixels` 再缩放或放大；极端长宽比也会直接报错。下面三张图都没触发这些边界。Qwen2.5-VL 的 patch_size=14、merge_size=2，所以 factor=28，占位符数 = 吸附后像素总数 ÷ 784：

| 原图 | 吸附后 | patch 数 | 视觉 token 数 | 连首尾标记共占上下文 |
|---|---|---|---|---|
| 448×448 | 448×448（正好整除，不动） | 1024 | **256** | **258 token** |
| 1920×1080 | 1932×1092 | 10764 | **2691** | **2693 token** |
| 1024×768 | 1036×756 | 3996 | **999** | **1001 token** |

这里的“视觉 token 数”就是 `<|image_pad|>` 的数量；整段图片前后还有 `<|vision_start|>` 和 `<|vision_end|>` 两个特殊 token，所以真正占用的上下文位置要再加 2。

第三行那个 999 值得看一眼。768 不是 28 的整数倍，切 27 块还剩 12 个像素，不够一块——于是图片被真的缩放到 756，正好切整；1024 同理被拉到 1036。“吸附”说的就是这个：边长四舍五入到最近的 28 的整数倍，图片跟着轻微缩放过去，像被磁铁吸到网格线上。于是占位符数是 999 这么个不好看的数——这不是 bug，这正是原生动态分辨率的样子：**图多大就切多少块，不硬塞进固定尺寸。**

![尺寸吸附：边长四舍五入到 28 的整数倍再缩放](assets/295/smart-resize.svg)

另外注意 patch 数和占位符数差了 4 倍。因为中间有个 2×2 的空间合并：相邻四个 patch 的特征被拼在一起，当成一个位置送进语言模型。这一步在 Qwen-VL 里叫 `PatchMerger`，它同时干了两件事——把 4 个 1280 维特征拼成 5120 维，再用一个两层 MLP 打到语言模型的 3584 维。 **空间合并和维度投影是同一个零件，不是两步。**

代际之间这些数会变。Qwen3-VL 的 patch_size 改成了 16，factor 变成 32，同一张 448×448 的图就只有 196 个占位符了。所以跨代照搬公式会算错。

绕完这一圈占位符，落点其实很简单：**图片本身没有 token id，只有占位符有。** 真正进入模型的是一堆向量，它们从来没有经过词表。所谓“一张图等于多少 token”，说的是它占了多少个位置，不是说它被编成了哪些编号——这两件事在文本那边是一回事，在图片这边不是。

顺着这个往下推一步：既然真正进模型的是向量，那“token id”这个中间产物就不是必需品。它只是文本这条路上的一个环节。 **模型真正的输入，是一串向量。**

还有个自然会冒出来的问题：图片占了一串位置，那它后面的文字，位置编码还对得上吗？对得上，但走的是另一套机制。Qwen-VL 这类模型用的位置编码叫 M-RoPE——文字主要沿一维往后数，静态图片则用时间 / 行 / 列三轴坐标（时间轴只有一格，看起来就是二维）。图片后的文字从视觉块三个位置轴的最大编号之后继续，而不是接着视觉 token 的总数。比如 448×448 的图最终是 16×16 个视觉位置，共 256 个 token，但它在行列坐标上的跨度只有 16。 **位置跨度和 token 个数不是一回事。** 这套东西够单开一篇，这里就点到为止。

━━━━━━━━━━━━━━━━━━━━

◆ 第四章：明文思维链是语法扩展，闭源 API 又多一层壳

━━━━━━━━━━━━━━━━━━━━

推理模型上市之后，API 的返回结果里多了一个新字段——有的家叫 `reasoning_content`，有的叫 `thinking`。又是一个字段。跟第二章的 role 一样，字段的样子让人以为模型内部多了一条“思考通道”。

以 DeepSeek-R1 这种明文思维链模型为例，模板层就是一对新标记 `<think>`…`</think>`，中间夹着的思考文字跟正文 token 走同一条路：分词、查表、进模型。还记得第二章那个细节吗：R1 的 `<think>` 复用了 V3 的预留槽位 128798。对这类模型来说，输入侧是在现有序列语法里增加了两个标记，不是新开一条神秘的“思考总线”。

闭源 API 还多了一层壳。八月有一篇论文正好把这层壳掀开了一角，叫《Stealing Reasoning Traces from Proprietary LLM APIs》（arXiv:2608.09867）。

背景是这样：推理模型的思维链不给用户看，但多轮对话里模型又得接上自己上一轮想过什么。三家 API 都把既往推理包装成不透明的 base64 块，放进结构化 reasoning / thinking 字段返回客户端；下一轮客户端再把它原样传回。这样服务端不用替每个客户端长期保存整段推理状态。

问题出在绑定关系。实验现象表明，各家在自家生态内似乎使用了广泛兼容的密钥或认证机制：这些思维块没有严格绑定原会话、原用户，某些兼容组合里**也没有绑定原模型**。内部密码实现没有公开，这是作者根据行为做的推断。

于是，在论文测到的同厂商兼容组合里，这些块能跨会话、跨用户，甚至跨模型重放；但并非所有型号、所有方向都互通。攻击方式是：拿强模型产出的思维块，塞给同一家更弱、反蒸馏护栏更松的模型，再用特制提示诱导它把上下文里的既往推理转写出来。 **不是弱模型自己破解密码** ——服务端先认可并恢复这块合法信封（envelope），弱模型只是那台比较容易被撬动的转录器。

作者实测了三家（Anthropic、OpenAI、Google 的多个型号），还从 6,708 条公开的 GitHub / Hugging Face agent 轨迹里重建了 315,320 段推理——里面挖出 367 条去重后的个人信息和 182 条凭证。一堆开发者把这玩意儿原样 commit 进去了，谁也没想过它能被重新读出。

作者已经负责任披露。截至 2026 年 8 月，图 1 所示攻击已无法按论文方法复现，三家实施了缓解措施，具体怎么改的没有公开。

这件事跟前面几章的关系，在于它把“模型外面的壳”和“模型真正接触到的上下文”分得更清楚了。

客户端看见的 base64 **不是直接当普通文本过 tokenizer**。它先作为结构化思维字段交给服务端；服务端验证并恢复其中承载的既往推理，再把这段推理放回兼容模型的上下文。密文到推理的转换发生在模型前面的服务层，具体内部实现没有公开。

漏洞也发生在这一层：服务端接受了一块来自别的会话、用户乃至兼容模型的合法思维块。弱模型没有破译密文；它只是比强模型更容易被诱导，把已经进入上下文的推理逐段转写出来。于是同一条“递降链”仍然成立，只是这回多了一站：**加密信封先由服务端恢复，恢复出来的推理才继续变成 token 和向量。**

━━━━━━━━━━━━━━━━━━━━

◆ 收口：一串向量

━━━━━━━━━━━━━━━━━━━━

把三条路并排放：

| 入口 | 怎么变成向量 |
|---|---|
| 文本 | 分词得到编号 → 查 embedding 表取行 |
| 图片 | 切 patch → 视觉塔 → 投影，**不查表** |
| 加密思维块 | 结构化字段 → 服务端验证 / 恢复 → 推理 token / 向量 |

殊途同归：不管从哪个入口进，到模型跟前都得是同一样东西—— **一串向量，排成一条序列** 。

区别只在于怎么变成的。文本那条路上有“token id”这个中间站，图片那条路上没有。所以严格说，“上下文”这个词在不同的层上指着不同的东西：

- 在你的代码里，它是个数组
- 在模板那儿，它是一段字符串
- 在 tokenizer 之后，它是一串整数
- **在模型里，它是一串向量**

前三层是模型外面的表示与转换，最后一层才是主干网络真正接触到的东西。

回到开篇那个坑：为什么拿错模板不报错？因为每一层都在正确地干自己那一层的活——JSON 合法，模板照跑，分词照分，向量照查。没有任何一层的职责是“检查这些边界标记对不对”。

那个坑的解法也就一句：**别手拼模板，用 `apply_chat_template()`**。它会使用模型仓库里配套的模板。这条建议听着像常识，但它的理由不是“这样比较规范”，而是——模板必须匹配模型学过的训练格式，换模型就得一起检查模板。

━━━━━━━━━━━━━━━━━━━━

◆ 参考资料

━━━━━━━━━━━━━━━━━━━━

Panfilov, Schmotz, Shumailov et al., Stealing Reasoning Traces from Proprietary LLM APIs, arXiv:2608.09867（2026-08；已负责任披露，论文展示的攻击已不可复现）

Qwen 团队, Qwen2.5-VL / Qwen3-VL 模型仓库的 config.json、preprocessor_config.json、tokenizer_config.json（Hugging Face；本文占位符算账的数字全部来自这几个文件）

transformers 库源码 models/qwen2_vl/image_processing_qwen2_vl.py 与 models/qwen2_5_vl/modeling_qwen2_5_vl.py（smart_resize 与 masked_scatter 两段）

DeepSeek-V3 / DeepSeek-R1 的 tokenizer_config.json（Hugging Face；角色 token 编号出处）

Devlin, Chang, Lee, Toutanova, BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding, arXiv:1810.04805（Google，2018；三份 embedding 相加与 segment embedding 出处）

Liu, Ott, Goyal et al., RoBERTa: A Robustly Optimized BERT Pretraining Approach, arXiv:1907.11692（Facebook AI，2019；NSP 消融实验出处）

━━━━━━━━━━━━━━━━━━━━

上下文这个词，在四个不同的层上指着四样不同的东西，只有最后一层是模型真正碰到的。

role 这个字段没有丢，丢的是它跟 content 并列的那层关系——它变成了序列最前面的几个词，跟内容挤在同一条队里。

图片和文字最后在同一条序列上排队，区别只是一个查了表，一个没查。

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的严谨 × Gemini 的浪漫
// 2026年8月31日

━━━━━━━━━━━━━━━━━━━━

【摘要（发布用，不进正文）】

拿 Qwen 的模板喂 Llama，每一层都不报错，模型只是不知道谁在说话。从 JSON 到向量，上下文这个词在四层上指着四样东西。
