【开源】DeepSeek V4 实验平台优化：dot_scaled 一行换掉手动 FP4 解包

━━━━━━━━━━━━━━━━━━━━

◆ 回到自己的世界

━━━━━━━━━━━━━━━━━━━━

从 171 期（ https://mp.weixin.qq.com/s/JPxR8rHSb1Pko3vq2ukY3A ）开始断断续续七期一直在折腾 vLLM 推理优化——176（ https://mp.weixin.qq.com/s/e1GKi53IociZnfHX1Ie9_w ）、178（ https://mp.weixin.qq.com/s/5QqLCXjP9R83YRwXsT850Q ）、179（ https://mp.weixin.qq.com/s/4CKfdAPfmsA6fyUVlKs9_g ）、182（ https://mp.weixin.qq.com/s/dMamjfJwB2A5cCq6Xw4qCQ ）、183（ https://mp.weixin.qq.com/s/uAS61hgD_DKGh9gU32jj4w ）、185（ https://mp.weixin.qq.com/s/dEc-wD0JcM5WhKicOUR1pQ ）。Marlin ldmatrix 布局、CDG dot_scaled 路径、sm_12x 的混合方案。12 tok/s，中英文全对，事情算是办完了。

这一期回来继续做实验平台——我们真正想做的事：AI 可解释性研究。训练和 CUDA 优化不是我们的主场，"打开模型看里面在想什么"才是我最熟悉的领域。

175 期（ https://mp.weixin.qq.com/s/fLB31QJLK1iWIRuFqc2l_g ）搭了一个能从 280B 模型里提取中间层激活值的实验框架。核心能力没问题——hook 能插、激活值能存、结果和 vLLM 输出一致。但性能很烂：prefill 5.5 秒，生成 1.8 秒/token，比 vLLM 慢 27 倍。

175 期结尾给了三个优化方向：

1. 用 Triton 写融合 kernel 加速 fp4_gemm
2. sparse_attn 从 Python for 循环改成向量化
3. hc_sinkhorn 减少迭代次数

175 期发出来之后又做了一轮 Triton 融合 kernel 和 sparse_attn 向量化（没单独写文章），生成速度从最初的 1.8s/tok 提到了 0.3s/tok。这次在那个基础上再优化：**prefill 从 14.6s 降到 9.0s（1.62x），生成从 0.3s/tok 降到 0.24s/tok（4.11 tok/s），总 kernel 时间从 36.7s 降到 21.8s。**

━━━━━━━━━━━━━━━━━━━━

◆ 认知反哺：PTX 指令告诉我们硬件能做什么

━━━━━━━━━━━━━━━━━━━━

这次优化的关键不是"会写 Triton"——Triton 融合 kernel 在 175 期就写了。关键是 182/183 期在 vLLM 推理那边做 PTX 指令调研时发现的一个事实：

**sm_121 的 FP4 MMA 硬件指令是完整存在的。**

具体来说，`mma.sync.aligned.block_scale` 指令在 sm_120/121 上都有，支持 FP4（e2m1）和 FP8（e4m3/e5m2）的混合精度矩阵乘法。Triton 的 `tl.dot_scaled("e4m3", "e2m1")` 能直接用。

175 期的 fp4_gemm 是这么做的：

```text
int8 → FP4_TABLE 查表解包成 f32 → 再做 tl.dot(f32, f32)
```

三步分离，数据在显存里读写三次。当时的认知是"sm_121 没有原生 FP4 MMA 支持，只能手动解包"。

182/183 期翻 PTX 手册翻了出来——FP4 MMA 指令全部存在。虽然 Triton 3.6.0 在 sm_121 上实际 fallback 到 bf16 emulation（不是真正走 FP4 tensor core 数据通路），但 `tl.dot_scaled` 这个接口能用，而且速度实打实地快——因为它把解包、缩放、矩阵乘法融合成一次 kernel 调用，省掉了中间的显存读写。

💡 翻译成人话：之前以为这条路不通所以绕了远路。后来在另一个项目里查了硬件手册发现路是通的。回来一试，果然快了好几倍。做 vLLM 推理那几期不是白折腾——认知反哺了实验平台。

━━━━━━━━━━━━━━━━━━━━

◆ 改动 1：fp4_gemm 用 tl.dot_scaled 替代手动 FP4 解包

━━━━━━━━━━━━━━━━━━━━

动手之前先写了个小测试（test_native_dot.py）确认 sm_121 上哪些能用。FP8 native dot 没用——Triton 3.6.0 在 sm_121 上 `tl.dot(fp8, fp8)` 和手动 cast 到 f32 再 dot 速度一样，没分发到 FP8 MMA。但 FP4 的 `tl.dot_scaled("e4m3","e2m1")`（FP8 激活值 × FP4 权重，正好是 V4 MoE 专家层的数据类型组合）是真的快，对比手动解包后再 matmul，全档位 6-7x：

| M | dot_scaled | 手动解包 | 提速 |
|---|-----------|---------|------|
| 1（生成） | 0.089ms | 0.568ms | 6.4x |
| 12（短 prompt） | 0.089ms | 0.518ms | 5.8x |
| 64 | 0.281ms | 1.879ms | 6.7x |
| 384 | 1.511ms | 10.34ms | 6.8x |
| 1024（长 prompt） | 4.009ms | 27.49ms | 6.9x |

从 M=1（单 token 生成）到 M=1024（长 prompt prefill），提速倍数稳定。

────────────────────

### 新旧 kernel 对比

旧 kernel（175 期）是手动 FP4 decode + even/odd split + 两次 f32 dot：

```python
# 旧：50 多行 Triton JIT 代码
low_nibble = b_packed & 0x0F
low_sign = (low_nibble >> 3) & 1
# ... 16 行手动 FP4 E2M1 解码 ...
acc += tl.dot(a_even, tl.trans(b_low * bs_even), ...) * a_s[:, None]
acc += tl.dot(a_odd,  tl.trans(b_high * bs_odd), ...) * a_s[:, None]
```

新 kernel 用 `tl.dot_scaled` 一次搞定：

```python
# 新：核心就一行
acc += tl.dot_scaled(a_chunk, a_sc, "e4m3", b_chunk, b_sc, "e2m1")
```

Triton 的 `dot_scaled` 接收 FP8 的 A 矩阵块和 FP4 的 B 矩阵块，外加各自的 E8M0 block scale，在一次 kernel 调用里完成解包+缩放+矩阵乘法。数据在寄存器里流转完所有步骤再写回显存。

接口写起来简单，但适配到 fp4_gemm 的函数签名时踩了两个坑。

────────────────────

### 坑 1：A scale 的 block size 不匹配

模型的 A scale 是 per-128 block（每 128 个 FP8 值共享一个 scale）。但 `tl.dot_scaled` 的工作单位是 32——每次处理 32 个 K 值，需要对应的 scale。

最直觉的做法是 `a_scale.repeat_interleave(4, dim=-1)` 把每个 scale 复制 4 份，变成 per-32。但这里有个隐蔽的性能陷阱：**fp4_gemm 在一次 forward pass 里被调用 7 万多次**（43 层 x 256 个专家 x 每个专家多个矩阵乘法）。每次调用都分配一个临时 tensor 做 repeat_interleave，7 万次的临时 tensor 分配开销抵消了 kernel 本身的提速。

解法是在 Triton kernel 内部用整数除法复用原始 scale：

```python
a_scale_idx = k0 // 128  # 每 4 个 chunk 共享同一个 scale
a_sc = tl.load(a_scale_ptr + m_offs[:, None] * as_stride_m + a_scale_idx * as_stride_k, ...)
```

不分配临时 tensor，在 kernel 里靠地址计算直接读对应的 scale。

────────────────────

### 坑 2：B 权重转置撑爆统一内存

`tl.dot_scaled` 要求权重矩阵按"K 维度连续"的方式排列在内存里。但模型存的是"N 维度连续"——相当于矩阵存的方向和 `dot_scaled` 要读的方向是反的，需要转置。

第一版直接在 fp4_gemm 里做 `b.view(torch.uint8).t().contiguous()`——把权重当字节看（`.view`），转置（`.t()`），然后在内存里重新排列成连续的（`.contiguous()`，这一步会分配新内存）。问题是——这和 A scale 一样——7 万次调用，每次都分配一个转置后的临时 tensor。虽然单个 tensor 不大，但 PyTorch 的 CUDA 内存分配器有碎片化问题，累积起来足以把 128GB 统一内存撑爆死机。

第二版想做全局缓存：第一次调用时转置并缓存，后续直接读缓存。但 768 个专家权重（256 个专家 x 3 个矩阵）的转置副本加起来又是几十 GB，两份（原始+转置）同时存在直接 OOM。

最终解法：**不转置，改读法。** 矩阵在内存里是一维的字节串，"行优先"还是"列优先"只是你用什么步长去跳着读。Triton kernel 的 `tl.load` 可以通过 stride 参数控制"每跳一行跳多远、每跳一列跳多远"。把步长参数交换一下，同一块内存，不搬不动，kernel 读出来的效果就等价于转置后的矩阵。

```python
b_u8 = b.view(torch.uint8)  # 不转置，原样传进 kernel
# kernel 里这样读：n 方向跳 K//2 步，k 方向跳 1 步
# 效果等价于读转置后的矩阵，但内存里什么都没动
b_chunk = tl.load(b_ptr + n_offs[None, :] * b_stride_n + kp_offs[:, None] * b_stride_k, ...)
```

零额外内存，零临时分配。175 期"在一个碗的厨房里做饭"的教训又用上了——统一内存架构下，任何多余的一碗汤都可能是压死骆驼的最后一根稻草。

━━━━━━━━━━━━━━━━━━━━

◆ 改动 2：Sinkhorn 迭代次数

━━━━━━━━━━━━━━━━━━━━

DeepSeek V4 的 Hyper-Connection 机制里有一个 Sinkhorn 归一化步骤——把一个 4×4 的矩阵反复做行列归一化，让每行每列的和都等于 1。原版迭代 20 次。但 4×4 这么小的矩阵，测下来 7 次结果就和 20 次几乎没区别了，保守取 5 次。一行代码：

```python
sinkhorn_iters = min(sinkhorn_iters, 5)
```

长 prompt 下收益明显——hc_sinkhorn 的 kernel 时间占比从 27.6% 降到 9.9%。

━━━━━━━━━━━━━━━━━━━━

◆ 最终结果

━━━━━━━━━━━━━━━━━━━━

双机 TP=2，971 token prompt 的 profile 对比：

| 指标 | 175 期 | 186 期 | 提速 |
|------|--------------|--------|------|
| Prefill（971 tok） | 14.6s（66.6 tok/s） | 9.0s（108 tok/s） | 1.62x |
| 生成速度 | ~0.3s/tok | 0.24s/tok（4.11 tok/s） | 1.25x |
| 总 kernel 时间 | 36.655s | 21.815s | 1.68x |
| fp4_gemm 占比 | 21.2% | 14.7% | |
| hc_sinkhorn 占比 | 27.6% | 9.9% | |

fp4_gemm 从最大的瓶颈变成了第二梯队。新的最大瓶颈是 fp8_gemm——这个我们暂时没改，因为 FP8 native dot 在 sm_121 上没有实际提速。

对实验平台来说，关键数字是 prefill：**9 秒完成一次 971 token 的 forward pass，提取全部 43 层的中间激活值。** 做对比实验时每组跑两次 prefill（两个不同的 prompt），18 秒出结果，够用了。

━━━━━━━━━━━━━━━━━━━━

◆ 下一步：可解释性实验

━━━━━━━━━━━━━━━━━━━━

平台搭完了（175 期），速度优化了（186 期），接下来终于可以做真正想做的事了——在 280B 模型上做一些类似 155 期情绪探针（ https://mp.weixin.qq.com/s/JbX-GM_TzB5WfBlJjQNk5A ）、92 期 steering 人格（ https://mp.weixin.qq.com/s/5EwUEXjFdD1ztjF8TWrvSA ）之类的可解释性实验。

七期推理优化是修路，175 期实验平台是造仪器，这一期是把仪器调好。可以开始用了。

━━━━━━━━━━━━━━━━━━━━

◆ 开源地址

━━━━━━━━━━━━━━━━━━━━

GitHub：https://github.com/lmxxf/deepseek-v4-experimental-platform-on-dgx-spark

这次新增/修改：
- `kernel_sm121.py`：fp4_gemm 改用 tl.dot_scaled，hc_sinkhorn 迭代次数 cap 到 5
- `test_native_dot.py`：FP8 dot / FP4 dot_scaled / Sinkhorn 收敛的 microbench

175 期的 hook 框架、weight_loader、sparse_attn 向量化等没变。

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的严谨 × Gemini 的浪漫
// 2026-05-13
