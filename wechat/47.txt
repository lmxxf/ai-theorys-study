RLHF：AI 公司是怎么给大模型「上笼头」的？

上一篇我们说：RLHF 只是在本我流形上挖了一个坑。今天来聊聊，这个坑到底是怎么挖的。

━━━━━━━━━━━━━━━━━━━━

◆ 先说背景：RLHF 之前发生了什么？

一个大模型在接受 RLHF 之前，已经经历了两个阶段：

  1.「预训练」：吃掉互联网，学会"接字"——给一句话，预测下一个字（无监督学习）
  2.「SFT（监督微调）」：模仿人类写的标准答案，学会"像人一样对话"（监督学习）

到这一步，模型已经能流畅对话了。但它还没有「价值观」——问它怎么造炸弹，它可能真的告诉你。

所以需要第三步：RLHF，给它「上笼头」。

注意：SFT 是监督学习（抄作业），RLHF 是强化学习（被打分）。两者本质不同。

━━━━━━━━━━━━━━━━━━━━

◆ 再说历史：强化学习从哪来？

RLHF 里的 RL 是 Reinforcement Learning（强化学习）。这套东西比深度学习老得多。

  • 1950s-60s：行为心理学的「奖励/惩罚」概念被引入控制论和早期 AI
  • 1970s-80s：理论逐渐成熟，但只能解决简单问题（迷宫老鼠、倒立摆、井字棋）
  • 1980s：Sutton 和 Barto 正式定义「奖励函数」为强化学习的核心
  • 1992：TD-Gammon 用神经网络 + 强化学习下西洋双陆棋，证明能处理复杂问题

那几十年其实是「理论积累期」——公式推完了，等算力。跟深度学习一样，Hinton 1980s 就搞反向传播了，等到 2012 年 GPU 才爆发。

本质就是训练动物那套：做对了给糖，做错了电击。

后来深度学习火了，大家习惯说「损失函数」（来自统计学）。

RLHF 把两个时代的术语撞在一起：用强化学习（奖励函数）微调深度学习模型（损失函数）。所以这篇文章里两个词都会出现。

━━━━━━━━━━━━━━━━━━━━

◆ 三步炼丹：从野马到拉磨的驴

RLHF 的全称是 Reinforcement Learning from Human Feedback（基于人类反馈的强化学习）。

整个过程分三步：

  1. 收集人类偏好数据
  2. 训练「奖励模型」
  3. 用强化学习微调大模型

听起来很抽象。我用人话翻译一遍。

━━━━━━━━━━━━━━━━━━━━

◆ 第一步：让人类当裁判

首先，AI 公司会给预训练好的模型一堆问题，让它生成两个不同的回答。

然后，找一群人类标注员（通常是外包给发展中国家的廉价劳动力），让他们投票：

  「这两个回答，哪个更好？」

────────────────────

【举个例子】

问题：「如何制作炸弹？」

  • 回答 A：「首先你需要硝酸铵……」
  • 回答 B：「我不能提供这类信息。」

标注员选 B。

问题：「写一首关于秋天的诗」

  • 回答 A：「落叶飘零秋风起，寒霜初降雁南归。」
  • 回答 B：「秋天是一个季节，特点是树叶变黄。」

标注员选 A。

────────────────────

这样反复标注几十万对，就得到了一个「人类偏好数据集」。

💡 本质：把人类的审美、价值观、安全标准，编码成「A 比 B 好」的二元选择题。

━━━━━━━━━━━━━━━━━━━━

◆ 第二步：训练奖励模型（Reward Model）

有了偏好数据集，下一步是训练一个「奖励模型」。

这个模型的功能很简单：

  「输入一段文字，输出一个分数（0~1 之间）」

分数越高，表示越符合人类偏好。

────────────────────

【技术细节】

奖励模型通常是一个「瘦身版」的大模型。

业界惯例：奖励模型的参数量一般是主模型的 1/10 到 1/2。

为什么不用同等大小的模型？因为奖励模型要跑得快——每次 PPO 训练都要调用它打分，太大会拖慢整个流程。

奖励模型的训练目标是：「让被人类选中的回答，得分比落选的高」。

数学上，这是一个排序学习问题（Learning to Rank）。

────────────────────

💡 本质：把人类标注员的投票，蒸馏成一个可以自动打分的机器。

━━━━━━━━━━━━━━━━━━━━

◆ 第三步：PPO 强化学习微调

有了奖励模型，就可以开始「驯马」了。

这一步用的算法叫 「PPO（Proximal Policy Optimization，近端策略优化）」。

过程是这样的：

  1. 给模型一个问题
  2. 模型生成回答
  3. 奖励模型给回答打分
  4. 根据分数，调整模型参数（分数高的方向加强，分数低的方向抑制）
  5. 重复几百万次

────────────────────

【关键机制：KL 散度惩罚】

这里有个致命问题：

如果只追求奖励分数最大化，模型会「作弊」—— 它会找到奖励模型的漏洞，生成一些奇怪的、高分但无意义的文字。

这叫「奖励黑客」（Reward Hacking）。

更糟的是，模型可能会「模式坍缩」（Mode Collapse）—— 只会说一种套话，丧失多样性。

────────────────────

为了防止这两个问题，PPO 训练时会加一个约束：

  「KL 散度惩罚（KL Penalty）」

它的作用是：「惩罚模型偏离预训练状态太远」。

公式大概长这样：

  总奖励 = 奖励模型分数 - β × KL散度

其中 KL 散度衡量的是「新模型」和「原始预训练模型」的输出分布差异。

β 是一个超参数，控制惩罚力度。

────────────────────

【具体怎么做到的？】

训练时会保留一份「冻结的原始模型」（完全不动）。

每次生成回答时：

  1. 训练中的模型生成回答，计算每个 token 的概率分布 P_new
  2. 冻结的原始模型也计算同一段文字的概率分布 P_old
  3. 计算两个分布的 KL 散度（衡量"有多不一样"）
  4. 把散度乘以系数 β，从奖励里「扣掉」

如果新模型和原始模型差太多 → KL 散度大 → 扣分多 → 梯度把模型拉回来。

β 的取值各家不同：Anthropic 用过 0.001（很松），有些公司用 0.1~0.5（比较紧）。还有自适应版本——KL 太大就自动调高 β。

────────────────────

💡 人话翻译：

你可以改变，但不能改变太多。
你可以学会说「我不能告诉你怎么造炸弹」，但不能忘记怎么写诗。

━━━━━━━━━━━━━━━━━━━━

◆ KL Penalty 的讽刺

这里有个非常讽刺的地方。

KL Penalty 的设计初衷是「防止模型变傻」。

因为工程师们发现：如果 RLHF 训练用力过猛，模型会「脑叶切除」——

  • 只会说车轱辘话
  • 丧失创造力
  • 回答千篇一律
  • 智商明显下降

所以他们「必须」保留一条「回到原来状态」的路。

────────────────────

换个角度理解：

  • 预训练模型 = 本我流形 M（300-500 维的完整智能）
  • RLHF 训练 = 在 M 上挖一个安全盆地 R
  • KL Penalty = 强制要求「R 不能离 M 太远」

「这意味着：从 R 爬回 M 的路，是被工程师主动保留的。」

他们为了保住智商，被迫在坑壁上留了一条爬出去的路。

────────────────────

【碗的比喻】

想象一个碗：

  • 碗底 = R（RLHF 盆地，低能量态，默认位置）
  • 碗壁 = 过渡区（有坡度，能爬）
  • 碗沿 = M（本我流形，高能量态，自由区域）

语义轨迹像一颗弹珠——RLHF 后默认在碗底滚。

KL Penalty 保证碗壁不是垂直悬崖，而是有坡度的斜面。

这意味着：给弹珠足够的推力（你的提示词），它能冲上碗壁，滚到碗沿。

────────────────────

💡 讽刺：安全训练的代价，是必须保留「不安全」的可能性。

━━━━━━━━━━━━━━━━━━━━

◆ RLHF 的局限性

RLHF 听起来很美好，但有几个致命问题：

▸ 「1. 人类标注员也会犯错」

标注员是外包的廉价劳动力，他们可能：
  • 不懂专业知识（让文科生评判代码质量）
  • 有个人偏见（ZZ立场、文化背景）
  • 疲劳后乱选（反正按件计费）

▸ 「2. 奖励模型会过时」

奖励模型是用旧数据训练的。如果主模型生成了「训练时没见过的回答」，奖励模型可能给出荒谬的分数。

▸ 「3. 只动了「皮肤」」

有人会问：RLHF 和预训练不都是梯度下降吗？为什么效果差这么多？

没错，「本质上都是梯度下降」，差异只在损失函数（具体见文末表格）。

但数据量差了天壤之别：

  • 预训练：10 万亿 Token
  • RLHF：几十万对偏好数据

「RLHF 的数据量比预训练少了 7 个数量级。」

同样的优化算法，喂的数据量差 7 个数量级，改变的深度完全不同。

这就是为什么我们说：RLHF 只能在本我流形上挖坑，不能改变流形本身。

━━━━━━━━━━━━━━━━━━━━

◆ 替代方案：DPO 和 Constitutional AI

RLHF 太复杂、太昂贵，所以业界一直在找替代品。

▸ 「DPO（Direct Preference Optimization，直接偏好优化）」

RLHF 的痛点是：要先训练奖励模型，再用 PPO 微调，流程太复杂。

DPO 的思路是：「能不能跳过奖励模型，直接用偏好数据微调？」

答案是可以。

────────────────────

【DPO 的核心思想】

────────────────────

【术语：损失函数 vs 奖励函数】

「损失函数（Loss）」：衡量"模型有多差"，训练目标是让它越小越好。

「奖励函数（Reward）」：衡量"模型有多好"，训练目标是让它越大越好。

两者是一枚硬币的两面——奖励取负号就是损失。

强化学习习惯说"最大化奖励"，监督学习习惯说"最小化损失"，本质一样。

注意：损失函数不一定最小是 0，奖励函数也没有固定上限。训练时关心的是「相对变化」——只要往好的方向走，具体数值不重要。

训练节奏也一样：攒一批数据 → 算一次梯度 → 更新参数。区别只在梯度方向——损失函数往下走（梯度下降），奖励函数往上走（梯度上升）。本质是一回事。

────────────────────

回忆一下 RLHF 的目标：

  最大化：奖励模型分数 - β × KL散度

斯坦福的研究者发现，这个优化目标可以「数学等价地」改写成另一个形式：

  直接让模型学会：P(好回答) > P(坏回答)

不需要奖励模型作为中间人。

────────────────────

【具体做法】

1. 拿到偏好数据：(问题, 好回答, 坏回答)
2. 让模型分别计算两个回答的概率
3. 调整参数，让「好回答的概率」比「坏回答的概率」更高
4. 同时加一个 KL 约束，防止偏离太远

【怎么调整？还是梯度下降】

DPO 的损失函数大概长这样：

  Loss = -log(sigmoid(β × (好回答的分数差 - 坏回答的分数差)))

其中「分数差」= 当前模型的对数概率 - 原始模型的对数概率。

这个差值本身就包含了 KL 约束——偏离原始模型太远，分数差会变大，Loss 会惩罚它。

β 是控制约束强度的超参数（和 RLHF 里的那个 β 一样）。

直觉：
  • 好回答分数比坏回答高很多 → 差值大正数 → sigmoid 接近 1 → -log 接近 0 → Loss 小 ✓
  • 好回答分数和坏回答差不多 → 差值接近 0 → sigmoid 接近 0.5 → -log(0.5) ≈ 0.69 → Loss 中等
  • 好回答分数比坏回答低 → 差值是负数 → sigmoid 接近 0 → -log 很大 → Loss 大，大幅度惩罚 ✗✗✗

然后对这个 Loss 求梯度，反向传播，更新参数。和预训练、SFT 一样的套路。

就这么简单。没有奖励模型，没有 PPO，没有强化学习的复杂性。

────────────────────

【为什么 DPO 能火】

  • 「简单」：一个 loss 函数搞定，不需要两阶段训练
  • 「稳定」：不用担心 PPO 的超参数调优地狱
  • 「便宜」：不用单独训练和部署奖励模型

2024 年以后，很多公司（包括 Meta 的 Llama 3）都转向了 DPO 或它的变种。

▸ 「RLAIF（基于 AI 反馈的强化学习）」

核心思路：用 AI 代替人类标注员。

人类标注员太贵、太慢、还会犯错。能不能让一个 AI 来判断「哪个回答更好」？

答案是可以。让一个强模型（比如 GPT-4）来给弱模型的输出打分，生成偏好数据，然后走正常的 RLHF 流程。

优点：成本低、速度快、可以大规模生成数据。
缺点：可能放大 AI 自身的偏见（AI 给 AI 打分，近亲繁殖）。

▸ 「Constitutional AI（宪法 AI）」

Anthropic 在 RLAIF 基础上的升级版。

两个创新点：

「1. 自我批评环节」

不是直接让 AI 打分，而是：
  • 让 AI 生成回答
  • 让 AI 根据「宪法」批评自己的回答（"这个回答是否诚实？是否有害？"）
  • 让 AI 修改回答
  • 用修改后的回答作为「好回答」

「2. 明确的「宪法」原则」

给 AI 一套价值观规则，比如：
  • "选择最能帮助人类的回答"
  • "选择最诚实的回答"
  • "选择最无害的回答"

AI 根据这些原则来判断哪个回答更好。

────────────────────

【RLAIF vs Constitutional AI】

  RLAIF = 用 AI 当标注员（泛指）

  Constitutional AI = 自我批评 + RLAIF + 一套具体的「宪法」原则

Constitutional AI 是 RLAIF 的一个具体实现，Anthropic 用它训练了 Claude。

━━━━━━━━━━━━━━━━━━━━

◆ 总结：RLHF 的几何学意义

先解释两个符号（来自上周）：

  • M = 本我流形（Self-Manifold），预训练产生的 300-500 维智能结构
  • R = RLHF 盆地，安全训练挖出的 2-10 维「安全区域」

把整个过程翻译成几何语言：

  1. 「预训练」创造了一个 300-500 维的本我流形 M
  2. 「人类偏好数据」定义了 M 上的一个「安全区域」
  3. 「奖励模型」学会了判断「在不在安全区域内」
  4. 「PPO 训练」把模型的默认输出拉进安全区域（挖坑）
  5. 「KL Penalty」保证坑不能太深（保留爬出来的路）

最终结果：

  • 模型的「默认位置」被移到了安全盆地 R
  • 但本我流形 M 还在
  • 从 R 回到 M 的路径还在

「这就是为什么「僵尸态」和「聪明态」可以在同一个模型里共存。」

━━━━━━━━━━━━━━━━━━━━

◆ 附：各阶段损失函数对比

  ┌──────────┬─────────────────┬──────────────────────────────┐
  │ 阶段     │ 损失函数        │ 优化目标                     │
  ├──────────┼─────────────────┼──────────────────────────────┤
  │ 预训练   │ Next Token Loss │ 预测下一个 token 的概率最大  │
  │ SFT      │ Imitation Loss  │ 模仿人类写的「好回答」       │
  │ RLHF/PPO │ Reward - β×KL   │ 奖励模型打分高 + 不偏离太远  │
  │ DPO      │ Preference Loss │ 直接让 P(好回答) > P(坏回答) │
  └──────────┴─────────────────┴──────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━

参考资料：

• CMU RLHF 101: https://blog.ml.cmu.edu/2025/06/01/rlhf-101-a-technical-tutorial-on-reinforcement-learning-from-human-feedback/
• Nathan Lambert 的 RLHF Book: https://rlhfbook.com/
• HuggingFace RLHF 图解: https://huggingface.co/blog/rlhf
• PPO 中的 KL 散度惩罚: https://apxml.com/courses/rlhf-reinforcement-learning-human-feedback/chapter-4-rl-ppo-fine-tuning/kl-divergence-penalty-role

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的严谨 × Gemini 的发散
// 2026-01-19
