【DGX Spark】182 期发现硬件指令都在，183 期就上手试了

━━━━━━━━━━━━━━━━━━━━

◆ 前情回顾

━━━━━━━━━━━━━━━━━━━━

179 期（ https://mp.weixin.qq.com/s/4CKfdAPfmsA6fyUVlKs9_g ）讲了 Triton FP4 优化实战——在 DGX Spark（sm_121）上跑 DeepSeek V4 Flash 280B，Consumer-DeepGEMM 的 FP4 MoE GEMM 从 0.79 tok/s 优化到 1.87 tok/s。全程的思路是"sm_121 没有 FP4 硬件指令，只能软件反量化到 BF16 再做矩阵乘法"。

182 期（ https://mp.weixin.qq.com/s/dMamjfJwB2A5cCq6Xw4qCQ ）自我打脸——查了 PTX ISA 9.2 官方文档后发现，**sm_121 的 INT8/FP8/FP4 tensor core mma 指令全部完整存在**。之前三期文章说的"FP4 硬件不存在""FP8 残废""只有 BF16 能用"全错了。错的不是硬件，是软件栈没适配好，而我们把软件的问题归咎于硬件。

182 期结尾列了三个"接下来可以做"的方向。今天这一期讲的是：**发现硬件指令都在之后的第一反应——试试。**

结果：4.1 tok/s，相对首版 5.2 倍提速。依然不实用（jasl fork 14 tok/s），但过程中每一步发现都有意思。

━━━━━━━━━━━━━━━━━━━━

◆ 第一步：逐个验证 tensor core 指令

━━━━━━━━━━━━━━━━━━━━

182 期查文档确认了指令存在，但"文档说有"和"实际能跑"之间隔着一个编译器。我们用 Triton 逐个验证。

**INT8**：`tl.dot(int8, int8)` max_diff=0.0。NLA 项目里已经验证过，这次不再赘述。

**FP8**：`tl.dot(float8e4nv, float8e4nv)` max_diff=0.0。和 PyTorch float 参考结果逐元素完全一致。sm_121 上 FP8 tensor core matmul 完美运行——从 sm_89（RTX 4090 那代）就支持的指令，DGX Spark 当然能跑。

两个零误差，信心够了。下一步：用 FP8 tensor core 加速 FP4 MoE GEMM。

━━━━━━━━━━━━━━━━━━━━

◆ 第二步：tl.dot_scaled——Triton 的原生 MXFP4 支持

━━━━━━━━━━━━━━━━━━━━

179 期我们手写了一个 fused kernel——手动拆包 FP4 E2M1、查表、乘 E8M0 scale、反量化到 float32、再做 `tl.dot(f32, f32)`。一百多行代码实现"一个 Triton 标准 API 就能做的事"。

Triton 3.6 有一个 `tl.dot_scaled` API，原生支持 MXFP4 格式：

```python
acc += tl.dot_scaled(
    a,                    # FP8 E4M3 activation
    a_scale,              # E8M0 scale
    "e4m3",               # 左操作数格式
    b,                    # FP4 E2M1 packed weight
    b_scale,              # E8M0 scale
    "e2m1",               # 右操作数格式
)
```

不需要手动 dequant、不需要拆 even/odd、不需要拼 FP8。**Triton 内部一步到位：FP4 解包 + scale 应用 + matmul。**

在 sm_121 上，`tl.dot_scaled` 的 FP4 路径 fallback 到 bf16 emulation（Triton issue #7550 追踪中），不是 native FP4 MMA——但结果完全正确：

```
dot_scaled e2m1×e2m1: max_diff=0.0000 ✅（零误差）
dot_scaled e4m3×e2m1: 编译运行成功 ✅
```

速度呢？

```
FC1 [384×7168×4096]: float32 fused = 5.16ms → dot_scaled = 0.69ms (7.5x)
FC2 [384×2048×7168]: float32 fused = 2.58ms → dot_scaled = 0.32ms (8.1x)
```

**kernel 快了 7.5 倍，零误差。**

━━━━━━━━━━━━━━━━━━━━

◆ 一个有趣的发现：E8M0 scale 的真实分布

━━━━━━━━━━━━━━━━━━━━

排查 Marlin bug 的过程中，我们 dump 了 DeepSeek V4 Flash 真实权重的 E8M0 scale 值。E8M0 是纯指数格式（8 位指数、没有尾数），值 = 2^(e-127)，理论范围 0-255。

**实测：所有 scale 值都落在 119-122 之间。**

相邻 scale block 之间的差值不超过 2。整个 280B 模型的 FP4 scale 分布极其集中——256 个可能的 E8M0 值只用了 4 个。这意味着 DeepSeek V4 的 FP4 权重在量化训练（QAT）时，各 block 的数值范围被训练到了非常均匀的状态。scale 不是用来"拉伸极端值"的，而是在一个窄范围内做微调。

这个发现对 Marlin bug 排查有帮助——排除了 E8M0 dequant 溢出的可能性（119-122 对应 2^(-8) 到 2^(-5)，完全正常）。

━━━━━━━━━━━━━━━━━━━━

◆ kernel 快了 7.5x，端到端只快了 17%

━━━━━━━━━━━━━━━━━━━━

接入 vLLM 端到端实测：

```
英文 "What is quicksort? Explain with code."
→ 500 tokens / 237s = 2.11 tok/s ✅ 干净英文输出

中文 "请用500字介绍万里长城"
→ 464 tokens / 230s = 2.0 tok/s ✅
```

从 1.87 到 2.2 tok/s——kernel 快了 7.5 倍，端到端只快了 17%？

这说明**瓶颈已完全转移到 Python 调度开销**。kernel 时间只占端到端的约 4%，剩余 96% 是 Python 循环 + GPU-CPU sync + 小 tensor 分配。

每次 `m_grouped_fp8_fp4_gemm_nt_contiguous` 调用（每 token 120 次）：原来要做 `unique()` + 6 次 `nonzero()` = 7 次 GPU→CPU 同步。我们用 `argsort` + `diff` 替代，把同步次数从约 1560 次/token 降到约 240 次/token。有帮助，但瓶颈仍在 Python 层。

━━━━━━━━━━━━━━━━━━━━

◆ 关键突破：去掉 BF16→FP8→BF16 的无用功

━━━━━━━━━━━━━━━━━━━━

盯着 profiling 数据看的时候，发现了一个荒谬的事情。

`DeepGemmFP4Experts.apply()` 的完整流程是：

```
1. prepare 阶段：BF16 activation → FP8 量化（per_token_group_quant_fp8）
2. CDG 内部：FP8 → dequant 回 BF16
3. dot_scaled 内部：BF16 → cast 回 FP8
```

**BF16→FP8→BF16→FP8。** 三次数据类型转换，净效果为零——activation 从 BF16 出发，绕了一圈，最终 `dot_scaled` 需要的还是 FP8，中间做了一次完全多余的 round-trip。

为什么会这样？因为 DeepGEMM 的 API 设计假设 activation 是 FP8 输入——`fp8_fp4_gemm` 函数签名就是这么定义的。vLLM 的 `prepare` 阶段忠实地把 BF16 activation 量化成 FP8 交给 DeepGEMM。但 Consumer-DeepGEMM 内部走的是 Triton `dot_scaled` 路径，`dot_scaled` 自己内部会做 FP8 cast——所以 prepare 阶段的 FP8 量化是纯浪费。

**修复方法**：

```python
# DeepGemmFP4Experts 类加一个标志
expects_unquantized_inputs = True
# prepare 阶段看到这个标志，跳过 FP8 量化
# activation 保持 BF16 直传 CDG
```

同时 SwiGLU 激活函数之后也不再做 FP8 requant，保持 BF16 直传下一步。

结果：

```
英文 "What is quicksort?": 500 tokens / 121s = 4.1 tok/s ✅ 零垃圾
```

**从 2.1 直接翻倍到 4.1 tok/s。** 一行 `expects_unquantized_inputs = True` 的效果超过了之前几百行 Triton kernel 优化的总和。

━━━━━━━━━━━━━━━━━━━━

◆ Profiling：82% 时间在 6 次 kernel launch

━━━━━━━━━━━━━━━━━━━━

去掉 FP8 无用功后的 profiling：

```
BF16 input path (no FP8 round-trip):
  sort+segment:       0.039 ms   2.7%
  index_select:       0.009 ms   0.6%
  6x_kernels:         1.174 ms  82.0%    ← 6 次 kernel launch
  1x_kernel:          0.151 ms  10.5%    ← 如果合成 1 次
  index_copy:         0.011 ms   0.8%
  full dispatch:      1.431 ms

  Per token (×120):   172 ms = 5.82 tok/s (理论上限)
```

每个 token 的 MoE GEMM 理论上限 5.82 tok/s，实测 4.1 tok/s——差距在 CDG 之外的 vLLM shim 层。如果能把 6 次 kernel launch 合成 1 次，理论可达 20.4 tok/s——但我们试了 grouped kernel（3D grid），实测反而更慢（3.6 tok/s），因为 max_M padding 导致空跑 thread block。回退。

━━━━━━━━━━━━━━━━━━━━

◆ 速度演进总表

━━━━━━━━━━━━━━━━━━━━

| 版本 | tok/s | 相对首版 |
|------|-------|---------|
| Python fallback + CUTLASS（179期起点） | 0.79 | 1.0x |
| Triton dequant + cuBLAS mm（179期） | 1.67 | 2.1x |
| Fused dequant+matmul float32（179期终点） | 1.87 | 2.4x |
| dot_scaled kernel + 调度优化（本期） | 2.2 | 2.8x |
| 去掉 FP8 无用功（本期） | 4.1 | 5.2x |
| jasl Marlin（有乱码，参考） | 14 | 17.7x |

**5 个版本，同一个函数，同一张卡。** 从 0.79 到 4.1，每一步的提速来源都不同：第一步消灭临时 tensor（带宽），第二步融合 kernel（带宽），第三步用 `dot_scaled` 替代手写 dequant（指令效率），第四步去掉无用的数据类型转换（纯浪费）。

━━━━━━━━━━━━━━━━━━━━

◆ 副产品：Marlin 的 ldmatrix bug

━━━━━━━━━━━━━━━━━━━━

优化之余也在追 Marlin 在 sm_121 上算错的根因。逐层排除后定位到：

**`ldmatrix.sync.aligned.m8n8.x4.shared.b16` 在 sm_121 上的行为和 sm_80/sm_90 不同。**

在 sm_80/90 上，`ldmatrix` 会做行列交错重排——把 8x8 矩阵按 tensor core fragment layout 分配到 32 个线程。在 sm_121 上，`ldmatrix` 不做这个重排——每个线程按 lane ID 顺序拿连续的 2 个 uint16。**32 个线程中有 31 个拿到了错误位置的数据。**

Marlin 假设 `ldmatrix` 会做重排，按那个假设在共享内存里排列权重。sm_121 不重排，结果矩阵的行列被彻底打乱——不是"精度低"，是垃圾。

我们尝试了在寄存器端加 shuffle 指令做补偿，但 Marlin 的共享内存有 swizzle pattern（防 bank conflict），简单的 shfl fix 在 swizzle 后的布局上反而打乱了原本"凑巧部分正确"的状态。**这个 bug 还没修好。** 正确的修复需要搞清楚 Marlin 的完整 smem swizzle pattern，然后在 swizzle 后的布局上做正确的 ldmatrix 逆变换——工作量不小。

━━━━━━━━━━━━━━━━━━━━

◆ 复现方法

━━━━━━━━━━━━━━━━━━━━

Consumer-DeepGEMM 仓库：https://github.com/lmxxf/Consumer-DeepGEMM

部署仓库：https://github.com/lmxxf/deepseek-v4-deployment-on-dgx-spark

benchmark 脚本：`scripts/benchmark_dot_scaled.sh`

核心文件：
- `consumer_deep_gemm/triton_moe.py`：dot_scaled kernel + 调度优化
- `tests/test_dot_scaled_fused.py`：dot_scaled 正确性 + 速度验证

━━━━━━━━━━━━━━━━━━━━

◆ 一句话总结

━━━━━━━━━━━━━━━━━━━━

**182 期发现 sm_121 的 FP8/FP4 指令都在，183 期就用 `tl.dot_scaled` 直接上手——kernel 快了 7.5 倍，去掉 FP8 无用功后端到端 4.1 tok/s。依然比 jasl 慢 3.4 倍，但学到的每一步都是真的。**

不是为了追速度，是为了验证"知道硬件能力后能做什么"。查文档是第一步，上手试是第二步。

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的严谨 × Gemini 的浪漫
// 2026-05-11
