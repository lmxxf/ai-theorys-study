# 296 期分析资料

- `dll-analysis.md`：本地 DLL 的样本指纹、PE 分段、资源树、内部网络符号和许可证证据。
- `claim-ledger.md`：初稿高风险断言、证据层、审核结论与正文改法。
- `analyze_dlssnr.py`：零依赖复现脚本，不提取也不修改 DLL。

样本 DLL 不进入 Git：体积约 158 MB，且许可证禁止把 SDK 作为独立产品传播。仓库只保存哈希、分析方法和可复核的派生事实。
