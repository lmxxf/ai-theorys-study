【DLSS5】nvngx_dlssnr.dll，老黄的一次意外的“开源权重模型”

━━━━━━━━━━━━━━━━━━━━

◆ 开篇：一个从篮球游戏里漏出来的文件

━━━━━━━━━━━━━━━━━━━━

2026 年 8 月底，NBA 2K27 放出抢先体验版。玩家没在里面打篮球，反倒在游戏目录里翻出一个不该在的文件：

`nvngx_dlssnr.dll`，158 MB。

这是 NVIDIA 还没发布的 DLSS 5 神经渲染（Neural Rendering）运行时，版本号 310.8.0.0。2K 打包发行版的时候，把一个预发布库一起塞了进去，没删。

从这个文件被数据挖掘者扒出来，到它在别的游戏里跑起来，中间隔了几个小时。到这周为止，社区已经把它塞进了 Control、赛博朋克 2077、荒野大镖客 2、霍格沃茨之遗、黑神话悟空、最终幻想 7 重生等十几款游戏——全部是非官方、老黄一点没参与的野生移植。

这篇不做深入剖析（那个留给后面几期，这东西够学一阵子的）。这期先把事情整体讲清楚：这个文件是什么、社区拿它干了什么、以及一个绕不过去的问题——**这算不算是老黄这辈子第一次“开源”了自己的旗舰模型。**

━━━━━━━━━━━━━━━━━━━━

◆ 第一章：DLSS 5 跟前四代不是一回事

━━━━━━━━━━━━━━━━━━━━

先说清楚 DLSS 5 是干什么的，因为它跟大家熟悉的 DLSS 不一样。

前四代 DLSS，核心是两件事：

```
超分辨率   低分辨率渲染 → AI 拉高到高分辨率
帧生成     少渲几帧 → AI 补出中间帧
```

一句话，前四代干的是**省算力**——让显卡少画点，AI 把缺的补上。所以开了 DLSS 帧率会涨，这是它的卖点。

DLSS 5 的神经渲染反过来了。它不是补缺，是**往画面里加东西**：拿到游戏已经渲染好的一帧，用一个 AI 模型重新改写里面的材质、光照、表面细节，让画面看起来更“真”。

方向一反，代价就来了。前四代是让显卡少干活，DLSS 5 是让显卡在原来的活之外**再多跑一个 AI 模型**。所以它不省帧，反而吃帧。

社区实测的数字很直接：

```
RTX 5070 Ti   Control 4K    71 → 35 FPS   掉一半
RTX 4090      Control       130 → 82 FPS  掉约 58%
```

掉帧不是 bug。你每一帧都在让显卡实体跑一遍神经网络，这是物理代价。（这个“为什么一定掉这么多”，跟模型有多大、每帧多少运算有关，是另一个够展开一整篇的话题。）

至于画质好不好，社区的评价是两极的。有人觉得光照更通透，也有一大片人觉得角色的脸被“AI 味”改得像换了个人——尤其皮肤，重绘之后容易滑进恐怖谷。这里有个客观原因：**这还是个没发布的预览版**，NVIDIA 官方明确说过，这些非官方实验不代表 DLSS 5 成品的画质和性能。拿半成品下结论，本来就不公平。

━━━━━━━━━━━━━━━━━━━━

◆ 第二章：社区几小时就让它跑了起来

━━━━━━━━━━━━━━━━━━━━

一个还没发布、只在 NVIDIA 内部测试的模型，社区凭什么几小时就让它在十几款游戏里跑起来？

因为它是**自包含**的。

这个 158 MB 的 DLL 里，装的不只是代码，还有整个神经网络的权重，以及为显卡预编译好的运算核心。换句话说，“模型是什么”和“怎么在显卡上跑”这两样东西，全打包在一个文件里了。你不需要去 NVIDIA 服务器下载什么、不需要联网、不需要额外配置——**拿到这个 DLL，模型就在你手里。**

这也是 DLSS 一直以来的惯例。玩过 PC 游戏的同学可能知道一个操作：把游戏目录里的 `nvngx_dlss.dll` 换成新版本，DLSS 的效果就升级了。能这么“换文件就换版本”，恰恰因为模型权重就在 DLL 里——不在里面的话，换个壳没有用。

于是社区做的事情，本质上就是**把这个文件搬到别的游戏里，再想办法让它接上那个游戏的渲染管线**。RenoDX 这个画质模组社区最先在 Control 上跑通，然后一款接一款地移植。

移植路上有个技术门槛值得一提。这个泄露版原本只能在 RTX 50 系（Blackwell 架构）上跑，因为 DLL 里那些预编译的显卡运算核心，是专门为 50 系编译的，40 系不认。

一位叫 Uncle Burrito 的模组作者解决了这个问题：他找出 DLL 里那些 40 系不兼容的运算核心，替换成兼容的版本，硬是让它在 RTX 4090、4080 上跑了起来。这一步很能说明问题——**它证明模型本身跟显卡架构是解耦的，卡住 40 系的只是那层预编译核心，换掉就行。** 模型权重照用不误。

到这儿，一个清晰的画面出来了：一个厂商还没发布的 AI 模型，权重、代码、运算核心全在一个能自由传播的文件里；社区不但让它跑了，还改了它的硬件目标、调了它的参数。这套流程，怎么看怎么眼熟。

━━━━━━━━━━━━━━━━━━━━

◆ 第三章：这不就是“开源权重”了吗

━━━━━━━━━━━━━━━━━━━━

眼熟在哪？眼熟在，这几乎就是开源模型社区的标准剧本。

一家放出模型权重 → 社区拿到手 → 各种二次开发：换硬件、调参数、塞进各种场景。DeepSeek、Qwen 这些开源模型，社区玩的就是这一套。

区别在于，DeepSeek 们是**主动**放出来的，还配了一整套东西：技术报告告诉你架构长什么样，说明文档告诉你怎么用，许可证告诉你能不能商用。

老黄这个 DLSS 5，是**被动**漏出来的，什么都没配。

────────────────────

💡 主动开源和事故泄露，差的是那套“知识”

同样是“权重能拿到手”，主动开源和这次的事故，差别不只在于是不是自愿。

真正的开源模型，给你的不只是一堆权重数字，还有一整套让你**看得懂、用得对**的知识：

```
技术报告   这个模型是什么架构、多大、怎么训的
建模代码   怎么把权重装进去跑起来
许可证     你能拿它做什么、不能做什么
```

这套知识才是最值钱的部分。有它，你能复现、能改进、能在别人的肩膀上往前走。

而 DLSS 5 这次漏的，只有权重和代码本身，那套知识一样没有。它的模型架构长什么样？没有文档，是社区靠逆向工程一点点猜出来的。许可证？游戏目录里那几个许可证文件是配套开发工具包的，压根没覆盖这个泄露的 DLL。你甚至不知道它是拿什么数据训出来的。

所以严格说，这是“权重开了，但知识没开”——**你拿到了发动机，但没有图纸、没有说明书、也没有授权。**

────────────────────

这里有个耐人寻味的对照。

一边，是主动把模型权重、技术报告、复现方法一股脑摆上台面的开源路线——这两年国内的 DeepSeek、Qwen 走的就是这条，把“怎么做到的”也讲给你听。

另一边，是全世界市值最高的 AI 硬件公司，自己的旗舰视觉模型，通过一次打包失误漏了出来，还得靠一群模组玩家逆向工程，外界才知道它大概是个什么结构。

要说清楚一点：老黄不开源 DLSS 5，这是正常的商业选择，不是什么原罪。DLSS 是 NVIDIA 显卡的核心卖点之一，捂着是本分。开源与否是一种选择，也是一种能力——DeepSeek 选择开源并且做得到，是它的路线；NVIDIA 选择闭源，是它的路线。两条路没有高下，只是这一次，闭源那条路上出了个事故，把底给露了。

于是就有了这个略带黑色幽默的结论：**老黄用一次 2K 的打包失误，完成了他这辈子唯一一次“开源权重模型”——非自愿、无文档、未授权的那种。**

━━━━━━━━━━━━━━━━━━━━

◆ 第四章：一个能学好几天的富矿

━━━━━━━━━━━━━━━━━━━━

对我这种“想搞懂 AI 到底怎么回事”的人来说，这个漏出来的文件是个意外的富矿。

平时我们学 AI，看的都是别人写好的论文、整理好的博客、封装好的接口。而这里摆着的，是一个**真实的、上线级别的、还没经过公关包装的**工业模型——它就是 NVIDIA 打算卖给全世界游戏玩家的那个东西本身。

把这个文件剖开，能顺着摸清楚一连串平时够不着的问题：

```
一个 158 MB 的文件里，代码和权重各占多少？
这个模型到底是什么网络结构？为什么是这个结构？
它每一帧从游戏里拿走哪些数据、又吐出什么？
为什么它只能吃满最新的显卡，还要为不同架构分别编译？
为什么它一定会让帧率掉这么多？
```

这些问题，每一个单拎出来都是一堂具体的课——比背概念实在得多。你面对的不是教科书里的示意图，是一个真会在几千万台显卡上跑起来的东西。

所以这不会是一篇就完的。这期先把来龙去脉和“事故式开源”这件事说清楚，后面几期我打算顺着这个文件往里走，一层层看它到底是怎么造出来的。

一次打包失误，给所有想学的人开了一扇本来关着的门。这大概是 2K 这个 bug 唯一的好处。

━━━━━━━━━━━━━━━━━━━━

◆ 参考资料

━━━━━━━━━━━━━━━━━━━━

VideoCardz, Breaking: Modders unlock experimental NVIDIA DLSS 5 hours after DLL discovery（2026-08；泄露发现与初期移植时间线）

VideoCardz / TweakTown, Leaked DLSS 5 already working with RTX 40 series（2026-08；Uncle Burrito 将 DLL 移植到 Ada 架构、RTX 4090 实测）

TechTimes, DLSS 5 Bleeds Frames in First Consumer Test: 71 to 35 FPS on RTX 5070 Ti（2026-08；RTX 5070 Ti 掉帧实测）

WCCFTech, NVIDIA DLSS 5 Neural Rendering In 10 Modern Games（2026-08；十余款游戏 ON/OFF 画质对比汇总）

━━━━━━━━━━━━━━━━━━━━

前四代 DLSS 让显卡少干活，DLSS 5 让显卡多跑一个模型——方向一反，帧率就从涨变成了掉。

模型的权重、代码、显卡运算核心全打包在一个 158 MB 的文件里，所以社区几个小时就能把它搬到别的游戏、甚至别的显卡上。

老黄没打算开源它，但一次打包失误替他开了——只开了权重，没开那套让你看得懂的知识。

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的严谨 × Gemini 的浪漫
// 2026年8月31日

━━━━━━━━━━━━━━━━━━━━

【摘要（发布用，不进正文）】

NBA 2K27 抢先版漏出了 DLSS 5 神经渲染的 158 MB 运行时，社区几小时就把它塞进十几款游戏、还移植到了 40 系显卡。权重、代码全在一个文件里——老黄意外“开源”了自己的旗舰模型。
