【KV Cache】上下文一压缩 AI 就跑偏：如果压的不是文字，而是 KV 呢

━━━━━━━━━━━━━━━━━━━━

◆ 开篇：我不懂汇编，但我盯着它就不跑偏

━━━━━━━━━━━━━━━━━━━━

这段时间我在做一个开源项目：把 NVIDIA DLSS 5 的神经网络移植到 AMD 的 9070 XT 上跑（ https://github.com/lmxxf/dlss5-on-amd-9070xt-porting ）。代码是两个 AI 轮班写的，我在旁边盯着。

GPU 汇编我看不懂，寄存器怎么分、缓存怎么排队，都是 AI 查资料、写测试、自己量出来的。按说我在不在场差别不大。可我心里清楚，它要是自己干，很快就会跑偏——我明明不懂，盯着到底起了什么作用？

跑偏最多的时刻，几乎都在同一个节点：**上下文压缩。**

对话一长，窗口快满了，编程工具会把前面的历史压成一段文字摘要，新开一轮让 AI 读着摘要接着干。压缩之前它还知道我们在干什么，压缩之后就像换了一个人，接手的是一份交接单。

交接单写得不算差。项目的开发日志一直在记，事实都在里面：测过什么、数字是多少、哪条改法采用了、哪条没采用。问题是日志越记越长，到 9 月 23 日已经 593KB，大约 30 万 token，新会话根本读不动。那天我让 AI 通读一遍、按主题重新整理，压到 22KB。

趁这个机会我回头翻了翻自己这些天的纠正，发现几乎没有一句是技术细节：

- “900P 和 1080P 的数字不能放在一起比。”
- “游戏锁了 30 帧，这个帧率说明不了网络快慢。”
- “主线是解释算力缺口在哪，不是零碎地提速。”

最后这句我说了不止一次。每压缩一回，AI 就又滑回“找个核优化一下”的状态。

我给不了汇编层面的意见，我给的是方向。于是冒出一个问题：**压缩丢掉的到底是什么？如果压缩的不是文字，而是模型内部的东西，能不能留住？**

━━━━━━━━━━━━━━━━━━━━

◆ 第一章：摘要丢的不是事实，是方向

━━━━━━━━━━━━━━━━━━━━

先说事实层面。压缩后的摘要里，该有的名词基本都有：哪几个核、哪几种改法、测出来的几个数。新会话的 AI 要查哪个数字，也能回日志里找到。

丢掉的是另一样东西：**这些事实之间谁轻谁重。**

哪条线已经证伪了、为什么证伪、证伪之后我们转向了哪里、现在的主线是什么。这些在原来的对话里不是某一句话，是几十轮来回慢慢堆出来的倾向：我否掉一个提议，它记住这个方向不行；我追问一个数字，它知道这里是重点。

摘要把这些压成一行“已尝试 X，效果不佳”。**名词留下了，坡度没了。** “效果不佳”是差 0.1% 还是方向根本错了，是“以后别碰”还是“等换了工具再试”，一行字里分不出来。

新 AI 读到一份没有坡度的清单，只能自己重建优先级。它拿什么重建？拿训练时学到的通用先验。而在“优化一个程序”这件事上，训练数据里出现得最多的，就是那几个常见的工程动作：找热点、改热点、再测一遍。

所以跑偏的方向几乎总是一样的。不是它笨，是它在缺信息的时候，做了统计上最稳妥的选择。**文字摘要保留了名词，丢掉了梯度，于是新 AI 顺着训练数据里的平均坡度往下滚。**

那把窗口加长行不行？30 万 token 读不动，那就做 100 万、200 万的窗口。

窗口加长只是把墙往后挪。2023 年斯坦福等机构那篇《Lost in the Middle》测过：关键信息放在长上下文的开头或结尾时，模型答得最好；放在中间，表现明显下降。另外，训练数据里真正需要把上百万 token 整合在一起才能答对的样本很少，模型没怎么练过这种事。

303 期（ https://mp.weixin.qq.com/s/HrrdYj-czN30_Fxrv_KNcw ）也碰到过这堵墙：窗口越长，塞满的代价是注意力被稀释。那期往下走，看的是把东西训进权重；这期换个方向，往模型内部的中间状态看。

━━━━━━━━━━━━━━━━━━━━

◆ 第二章：KV cache 是什么，为什么它在“坍缩”之前

━━━━━━━━━━━━━━━━━━━━

先把机制摆出来，再说名字。

一个 token 进入模型，要穿过几十层。每一层里做注意力的时候，这个 token 会被乘上三个矩阵，得到三个向量：

```
q = x · W_Q     # 查询：我想找什么
k = x · W_K     # 键：我是什么，供别人来比
v = x · W_V     # 值：别人找到我之后，从我这儿拿走什么
```

后面的 token 生成时，拿自己的 q 去和前面每一个 token 的 k 比相似度，再按相似度把前面的 v 加权求和。所以前面每个 token 的 k 和 v 算一次就够了，存起来，后面的 token 反复来查。

这些存起来的 K、V 向量，就叫 键值缓存（key-value cache，KV cache）。168 期（ https://mp.weixin.qq.com/s/YqcTnIrGKEYZ2n-sMAelKg ）从 MHA 讲到 MLA 时，把它当主角讲过：推理时显存的大户，注意力这几年的改进大多冲着它去。

它有多大？拿 Llama-2-7B 算一下：32 层，每层的 K 和 V 各是 4096 维。

```
每个 token 的 KV = 32 层 × (4096 + 4096) = 262,144 个数
按 FP16 存，每个数 2 字节 → 约 512 KB
```

**一个 token 写成文字只占几个字节，它在模型里留下的 KV 有半兆。**

────────────────────

再看文字摘要是怎么来的。摘要也是模型生成的，它要一个字一个字地往外吐。每吐一个字，最后一层的向量要过输出层，变成一张覆盖整个词表的概率分布，再从里面采一个词出来。

**这一步是坍缩。** 中间层里可能同时存在好几种倾向：“这条路不太行”“但也许换个工具能行”“他好像很在意这件事”。到了输出层，只有被采中的那个词留下来，其他的倾向如果没被说出口，就过不去。

KV 不一样。**它是每一层在坍缩之前算出来的东西。** 那些没说出口的倾向，至少有机会以某种形式留在这几十万个数里。

于是我的问题就可以写得更具体一点：**压缩上下文的时候，不压文字，压 KV，是不是就有一种相对连续的记忆？** 甚至，有些“感觉”说不定一个 token 的 KV 就能记下来——毕竟它有半兆的空间。

────────────────────

💡 服务器上已经在缓存 KV 了，那算不算记忆

用 API 的人可能听说过 提示缓存（prompt caching）。上文 168 期算过它的价格：你每次发请求，前面那一大段系统提示和历史对话都一样，服务端如果还留着上一轮算好的 KV，这一轮就不用重算，命中缓存的输入便宜很多。

这确实是在服务器上存 KV。但照 Anthropic 官方文档的说法，它的性质很明确：默认缓存寿命 5 分钟，每用一次免费续期，另有加价的 1 小时档；命中要求前缀逐字完全相同；不同组织之间从不共享缓存。

**它存的是“这段话我算过了”，不是“我从这段话里记住了什么”。** 没压缩，一个字不同就不命中，过期就丢。它省的是算力，不是记忆。

────────────────────

要让 KV 变成记忆，至少得压缩，还得能跨过会话留下来。这件事有人做过。

━━━━━━━━━━━━━━━━━━━━

◆ 第三章：已经有人这么压过

━━━━━━━━━━━━━━━━━━━━

查了一圈，“不压文字、压中间状态”这条路，2022 年到 2024 年间有好几篇工作走过。它们压的东西不完全一样，有的直接留 KV，有的留的是一组向量、再喂回模型重新变成 KV，但共同点是：**都不经过输出层的采样。**

**第一篇，Gist tokens。** 斯坦福的 Mu、Li、Goodman，NeurIPS 2023。做法是在提示后面插几个特殊的 “gist” token，然后改注意力掩码：gist 后面的内容**看不到**原提示，只能看到 gist token。这样训练下来，模型被迫把原提示的信息挤进 gist token 里。推理时只存 gist token 的 KV，原提示可以扔掉。

论文报告最多 26 倍的提示压缩，计算量最多省 40%。他们试了 1、2、5、10 个 gist token，发现压成 1 个并不比多几个差多少，后面的实验就都用 1 个。

一个 token 的 KV，装下一整段指令。这正是我想象的那种情形。只是要看清它压的是什么：训练数据是指令微调那类短提示，论文自己也列了失败例子，碰到需要逐字照搬的细节时会丢。

**第二篇，AutoCompressor。** 普林斯顿的 Chevalier、Wettig、Ajith、陈丹琦，EMNLP 2023。把长文档切成段，每段末尾接一组特殊 token，取它们最后一层的输出当“摘要向量”，作为软提示接到下一段前面。前面所有段的摘要向量都累积带着往下走。常用配置是每 2048 个 token 的一段压成 50 个向量，在 OPT 和 Llama-2 上微调，训练序列最长 30,720 个 token。

**第三篇，In-Context Autoencoder（ICAE）。** 微软的 Ge 等人，ICLR 2024。用一个加了 LoRA 的模型当编码器，把 512 个 token 压成 128 个“记忆槽”，交给原封不动的同一个模型去读。4 倍压缩，额外参数约 1%。它用自编码训练：要求模型能从记忆槽里把原文复述出来。

**第四篇，Recurrent Memory Transformer（RMT）。** Bulatov、Kuratov、Burtsev，NeurIPS 2022。更早一点，在每段输入里加几个记忆 token，这段处理完，记忆 token 的输出传给下一段当输入。一段一段接力，信息靠这几个 token 往后带。

────────────────────

另一类不压缩，只挑。

**H2O**（Zhang 等，NeurIPS 2023）观察到，注意力的分数大多集中在一小部分 token 上，就只留这些“重击手”（heavy hitters）和最近的 token 的 KV，其他的扔掉。

**StreamingLLM**（Xiao 等，ICLR 2024）发现开头那几个 token 不管内容是什么都会吸走大量注意力，只要留住开头 4 个 token 的 KV 加上最近一段窗口，模型就能不经微调持续跑下去。

这两个是按规则裁剪，不训练压缩器。**它们解决的是显存装不下，不是记住什么。** 被扔掉的就没了。

────────────────────

最后是已经量产的形态：**MLA**。DeepSeek-V2 的多头潜在注意力（Multi-head Latent Attention，MLA），把每个 token 的 K、V 先压进一个 512 维的潜向量存起来，用的时候再展开，另外单独带 64 维放位置信息。论文报告 KV cache 比原来的 DeepSeek 67B 少了 93.3%。312 期（ https://mp.weixin.qq.com/s/n9v-PvAT_fDIFLd-t05FDg ）讲秩的时候碰过这个 512：它是一个故意造出来的低秩。

MLA 压的是每个 token 自己的 KV，token 的条数没少。它是省显存的办法，但它说明了一件事：**KV 本来就可以压，模型可以从训练一开始就学会读压缩后的形式。**

把这几条摆在一起：

| 做法 | 压什么 | 需要训练吗 | 留下的是什么 |
|---|---|---|---|
| Gist tokens | 提示 → 1 到 10 个 token | 要，改掩码微调 | gist token 的 KV |
| AutoCompressor | 每段 2048 → 50 个向量 | 要，微调 | 逐段累积的摘要向量 |
| ICAE | 512 → 128 个记忆槽 | 要，LoRA 编码器 | 记忆槽向量 |
| RMT | 段 → 几个记忆 token | 要 | 段间接力的记忆向量 |
| H2O / StreamingLLM | 按规则挑一部分 | 不要 | 原样的一部分 KV |
| MLA | 每个 token 的 KV → 512 维 | 从预训练起 | 压缩后的 KV |

这些工作针对的场景大多是省算力、塞下更长的文档，**没有一篇是冲着“让 AI 跨过压缩还记得方向”去做的。** 但它们证明了前半截：坍缩之前的中间状态，可以被压得很小，模型也能读回来。

━━━━━━━━━━━━━━━━━━━━

◆ 第四章：三个硬问题

━━━━━━━━━━━━━━━━━━━━

能压，能读，那为什么我的编程工具还在写文字摘要？往下想，碰到三个问题。

**第一，KV 是私有坐标。**

KV 是 `x · W_K` 和 `x · W_V` 乘出来的。W_K、W_V 是这个模型自己的权重，换一个模型，矩阵不一样，同样一串数字在它那里毫无意义。哪怕是同一家的新版本，只要权重重训过，旧的 KV 就作废。

文字不一样。文字是公共协议：我的开发日志，Codex 能读，Claude 能读，明年的新模型也能读。这个项目本身就是两个 AI 轮班，它们之间唯一能交接的，就是文字。

**第二，人读不了。**

开篇说过，防跑偏靠的是一个不会被压缩的人在旁边看方向。我看不懂汇编，但我读得懂交接单：它写“主线是找热点提速”，我一眼就能看出偏了，改一句就行。

KV 记忆偏了，我看不出来，也改不了。那是几十万个浮点数，没有哪一格写着“主线”。307 期（ https://mp.weixin.qq.com/s/otABJNNe7tdCJGUcv2AfAA ）讲过一个说法：**不用很懂，但不能不知道。** 保安大爷不懂业主家里的事，但知道小区里什么不该出现。KV 记忆把保安亭也收走了，因为人连“正常长什么样”都看不见。

**第三，模型得学会读。**

上面那几篇，除了 H2O 和 StreamingLLM 这种只裁剪的，全都要额外训练：Gist 要改掩码微调，AutoCompressor 要微调，ICAE 要训编码器，MLA 干脆从预训练就这么练。一个现成的模型，你直接塞给它一包压缩过的 KV，它不认。

位置也是个麻烦。现在的模型大多用 旋转位置编码（Rotary Position Embedding，RoPE），它把 token 的位置直接转进 K 向量里。KV 存下来的时候，位置就焊死在上面了。DeepSeek-V2 论文专门讲了这个冲突：RoPE 对位置敏感，和低秩压缩放在一起，推理时就得把前面所有 token 的 K 重新算一遍，所以 MLA 把位置单独拎出来，用那额外的 64 维带着。StreamingLLM 也绕了一道：它存的是加位置之前的 K，每次按 token 在缓存里的位置重新转，而不是按它在原文里的位置。

把一段旧会话压缩后的 KV 接到新会话前面，这些位置怎么排、和新内容怎么对齐，每一种方案都得自己处理。

────────────────────

💡 这三个问题，其实是同一件事的三面

私有、不可读、要训练，说到底都因为 KV 属于模型内部的坐标系。

文字之所以公共、可读、不用训练，恰恰因为它经过了输出层的坍缩，被投影到了人和所有模型共用的那张词表上。**它丢方向的原因，和它能被所有人读懂的原因，是同一个。**

反过来，KV 能留住没说出口的倾向，也是因为它没被投影到那张公共词表上。好处和坏处连在一起，没法只要一半。

────────────────────

既然只能要一整个，那就别让它单独上岗。

━━━━━━━━━━━━━━━━━━━━

◆ 收尾：日记和睡眠

━━━━━━━━━━━━━━━━━━━━

所以我现在觉得，合理的形态不是二选一，而是两层并存。

**文字一层，像日记。** 可以审计，可以跨模型交接，人能读、能改。事实、数字、证伪过的路、为什么证伪，写在这里。它会丢坡度，但坡度丢了，人能看出来、补一句。我那份从 593KB 压到 22KB 的日志，就是这一层。

**压缩 KV 一层，像睡眠。** 只给同一个模型自己用，存那些说不清的倾向：哪里是重点、哪里已经走过、我在意什么。人读不了，也不需要读，就像人不会去读自己睡觉时大脑里重放了什么。换模型的时候它作废，日记还在。

两层之间的分工也清楚：**日记负责对，睡眠负责顺。** 睡眠那层偏了，人从 AI 的行为上看出来，拿日记纠正它。

────────────────────

我一直有个说法：**一切都是上下文，只是刷新周期不同。** KV cache 是分钟级的，服务器上 5 分钟不用就丢；会话里的记忆是会话级的，窗口关了就没了；权重是训练级的，几个月才动一次。

压缩 KV 做成的记忆，正好填在会话和权重之间：比一次会话活得久，又不用动权重。

上文 303 期查完一圈，收在一句话上：筛选那一侧出了一堆办法，写入那一侧基本还是同一招——另开一小块参数，把东西灌进去。压缩 KV 换了一个写入的地方：**不写参数，写激活。** 不用梯度下降，不怕学新忘旧，删掉一包 KV 就是真删掉。

代价是它只认一套权重，人也看不懂。所以它替不了日记，只能和日记一起用。

我的项目还在跑，两个 AI 还在轮班。眼下能做的，还是把那份 22KB 的日志写准：少写名词，多写坡度——哪条线死了、为什么死、现在往哪走。

━━━━━━━━━━━━━━━━━━━━

◆ 参考资料

━━━━━━━━━━━━━━━━━━━━

Mu, Li, Goodman, Learning to Compress Prompts with Gist Tokens, arXiv:2304.08467（斯坦福，NeurIPS 2023；改注意力掩码让提示信息挤进 gist token，只缓存 gist token 的 KV；LLaMA-7B 与 FLAN-T5-XXL 上最多 26 倍提示压缩、最多 40% 计算量节省；试过 1、2、5、10 个 gist token，1 个不明显差于更多，后续实验用 1 个）

Chevalier, Wettig, Ajith, Chen, Adapting Language Models to Compress Contexts, arXiv:2305.14788（普林斯顿，EMNLP 2023；AutoCompressor，逐段生成摘要向量作软提示并累积；常用每段 2048 token 压成 50 个向量，OPT 与 Llama-2 上微调至 30,720 token 序列）

Ge, Hu, Wang, Wang, Chen, Wei, In-context Autoencoder for Context Compression in a Large Language Model, arXiv:2307.06945（微软，ICLR 2024；LoRA 编码器生成记忆槽，未改动的原模型作解码器；512 压成 128，4 倍压缩，额外参数约 1%）

Bulatov, Kuratov, Burtsev, Recurrent Memory Transformer, arXiv:2207.06881（NeurIPS 2022；在输入里加记忆 token，段间循环传递）

Zhang et al., H2O: Heavy-Hitter Oracle for Efficient Generative Inference of Large Language Models, arXiv:2306.14048（NeurIPS 2023；保留注意力贡献大的 token 与最近 token 的 KV，其余逐出）

Xiao, Tian, Chen, Han, Lewis, Efficient Streaming Language Models with Attention Sinks, arXiv:2309.17453（ICLR 2024；保留开头 4 个 token 与最近窗口的 KV，不经微调；缓存加旋转位置编码之前的 K，按缓存内位置重新编码）

DeepSeek-AI, DeepSeek-V2: A Strong, Economical, and Efficient Mixture-of-Experts Language Model, arXiv:2405.04434（2024-05；MLA 把 KV 压进 512 维潜向量，另以 64 维解耦承载 RoPE；KV cache 较 DeepSeek 67B 减少 93.3%；论述 RoPE 与低秩 KV 压缩的冲突）

Liu et al., Lost in the Middle: How Language Models Use Long Contexts, arXiv:2307.03172（TACL；相关信息在长上下文开头或结尾时表现最好，在中间时明显下降）

Anthropic, Prompt caching 官方文档（默认 5 分钟寿命、使用即免费续期、另有 1 小时档；前缀须逐字相同；组织之间不共享缓存）

Llama-2-7B 模型配置文件（32 层，隐藏维度 4096，32 个 KV 头；用于正文每 token KV 大小的估算）

━━━━━━━━━━━━━━━━━━━━

文字摘要留下了名词，丢掉了坡度；新来的 AI 只好顺着训练数据里的平均坡度往下滚。

文字丢方向的原因，和它能被所有人读懂的原因，是同一个。

日记负责对，睡眠负责顺；睡眠偏了，拿日记纠正它。

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的文笔 × GPT 的严谨 × Gemini 的浪漫
// 2026年9月25日

━━━━━━━━━━━━━━━━━━━━

【摘要（发布用，不进正文）】

两个 AI 轮班写代码，一压缩上下文就跑偏：文字摘要留下名词，丢掉方向。如果压的不是文字而是 KV 呢？Gist、ICAE 等工作证明坍缩前的状态能压能读，但 KV 只认一套权重、人也读不了。合理的形态是两层并存：文字当日记，KV 当睡眠。
