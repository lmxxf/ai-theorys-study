【AI可解释性】用JSON向AI提问效果更好？真的假的？

━━━━━━━━━━━━━━━━━━━━

◆ 开篇：一个刷视频刷来的偏方

━━━━━━━━━━━━━━━━━━━━

前一阵刷视频，看到一些 up 主在推荐一个提示词偏方：**跟 AI 提问别写大白话，把问题组织成 JSON 格式发过去，回答质量会明显变好**。英文圈也有同款，Medium 上的标题起得更夸张——"ChatGPT 高手不想让你知道的技能：JSON Prompting"。理由通常是一句话："模型吃过海量代码和 API 文档，JSON 对它来说是高信号格式。"

我寻思着这事儿有没有道理。它闻起来有点玄学——但又不完全像玄学：写过 LLM 应用的程序员都知道，API 里确实有个正经功能叫 JSON mode / structured output（结构化输出），让模型保证输出合法 JSON。**偏方说的是输入侧（用 JSON 问），官方功能管的是输出侧（让模型答 JSON）**——两边都绕着同一个问题：格式这个东西，对模型来说到底意味着什么？

先交代一下本文的路线，省得读到一半迷路：**"JSON 输入会不会让回答更好"，据我所知没有人正经研究过**——但**输出侧**这两年被学术圈研究透了：从"强制输出格式会不会伤智商"的争论，一路走到 2026 年一篇预印本的发现——格式在模型内部竟然可以浓缩成**一根方向向量**。而输入和输出侧共用同一个东西：模型内部的"格式状态"。所以本文的打法是**绕道**：先把输出侧的机制搞清楚，最后回来对输入侧的偏方给一个有根据的猜测。

还要交代第二句：下面这几篇论文，每篇只负责自己那一小块证据，把它们串成一条能回应偏方的逻辑线，是我自己的拼法——故事是我编排的，证据是它们各自的。拼得成不成立，你看完自己判断。

下面出发，从工程实现讲到内部机制，最后回来看那个偏方。

━━━━━━━━━━━━━━━━━━━━

◆ 第一节：JSON mode 的工程实现——掐着嘴巴逐 token 过滤

━━━━━━━━━━━━━━━━━━━━

先讲机制。主流的结构化输出实现，学名叫 grammar-constrained decoding（语法约束解码），代表作是 Outlines（arXiv 2307.09702）。

基本思路一句话就能说完：生成的每一步，把不符合 JSON 语法的 token 的 logits 置成 -inf，只在合法的 token 里采样。这一步谁都能想到，**难的不是掩码，是"哪些 token 合法"这个判断本身**，有两个坑：

第一个坑：**合法性取决于"现在写到语法的哪个位置"**。同一个 token `"` ，在等着写 key 的位置是合法的，在刚写完 `{` 等着写 key 的位置也是合法的，但在一个数字写到一半的位置就不合法。所以得有个东西记住"当前语法状态"——这就是有限状态机（FSM）：写完 `{` 进"期待 key"状态，写完 key 进"期待冒号"状态，一步步转移。JSON 语法简单，状态机也就几十个状态。

第二个坑（这个才要命）：**模型的 token 和语法的字符对不齐**。词表里的 token 是 BPE 切出来的多字符碎块——我翻了下 Qwen 的词表（15.1 万个 token），里面真实存在 `{"`、`":"`、`"},` 这样的碎块，最夸张的是 `},{"`：一个 token，横跨"关掉上一个对象 → 逗号 → 打开新对象 → 准备写 key"四个语法状态。判断这种 token 在某个状态下合不合法，得让它的每个字符在状态机上连走四步、步步不违规才行。而词表有十几万个 token，**每生成一步都对全词表做一遍这种模拟，慢到没法用**。

Outlines 的核心贡献就是解决第二个坑：**离线预处理**。把词表里每个 token，从状态机的每个状态出发都试走一遍，提前建好一张大表——"状态 S 下，合法的 token 集合是 {...}"。生成时每步只需查表，掩码开销近乎为零。所谓"把语法编译成 FSM"，实际编译出来的是这张 状态 × 词表 的索引表。

另外记住一个关键事实，后面要用：**这一切发生在输出层，模型本体一个参数都没动，模型甚至不知道自己被约束了**。

────────────────────

💡 这里藏着一个不显然的副作用

硬掩码会**扭曲模型本来的概率分布**。Grammar-Aligned Decoding（arXiv 2405.21047，NeurIPS 2024）指出：把不合法 token 掐掉再归一化，得到的分布并不等于"模型在所有合法输出上的真实条件分布"——模型本来沿着某条高概率路径走，被掩码硬拐进一条它自己觉得别扭的路径，后续每一步都是在"将错就错"。语法是合法了，但生成的内容可能已经不是模型的最优解。

────────────────────

打个比方：模型每一步"内心"都有一个完整的候选发言列表，约束解码就是站在旁边掐着它的嘴，凡是不符合语法的词一律不准出口。模型可能很想先说一句 "Sure, here's the JSON:"——不准，第一个字符必须是 `{`。

被掐着嘴说话，能不影响发挥吗？这正是接下来这场架吵的东西。

━━━━━━━━━━━━━━━━━━━━

◆ 第二节："格式税"之争——强制 JSON 到底伤不伤推理

━━━━━━━━━━━━━━━━━━━━

有了上面的机制背景，就能看懂这场争论了。

**正方**：Let Me Speak Freely（arXiv 2408.02442，EMNLP 2024 Industry track）。论文标题就是态度——"让我自由说话"。他们的实验结论是：强制 JSON / XML / YAML 格式输出，会显著伤害模型的推理能力，而且**约束越严格，退化越大**。直觉解释也说得通：推理类任务需要模型"先想后答"，而格式约束从第一个 token 就开始掐嘴，把"想"的空间挤没了。

**反方**：Outlines 背后的 dottxt 团队，Will Kurt 写了篇博客反驳（blog.dottxt.co，标题 "Say What You Mean"）。核心论点：你们观察到的退化，锅不在约束解码本身，而在**烂 prompt 和烂 schema**——比如 schema 设计得没给推理留字段、prompt 和格式要求打架。把 prompt 和 schema 写好，约束解码不但不伤，还常常更好。

**裁判视角**：JSONSchemaBench（arXiv 2501.10868）拿上万个真实世界的 JSON schema 系统评测了各家约束解码框架，比的不只是"语法合法率"，还有对下游任务质量的影响。结果大体上是：不同框架、不同任务差异很大，"格式税"不是一个恒定存在的东西，但也确实不是空穴来风。

老实说，两边都有道理。正方指出的现象是真的——你如果用过早期的 JSON mode，大概率也遇到过"套上格式之后答案明显变笨"的时刻，这不怪你的错觉。反方指出的归因问题也是真的——很多退化案例换个 schema 就消失了。**这场争论真正缺的，是打开模型内部看一眼：格式处理到底发生在哪里、和内容处理是不是纠缠在一起。**

这就要先补一块背景知识。

━━━━━━━━━━━━━━━━━━━━

◆ 第三节：拒绝、人格、指令——一根向量就能开关一种行为

━━━━━━━━━━━━━━━━━━━━

可解释性研究这几年反复发现一件事：模型的很多**高层行为**，在内部激活空间里对应的不是一团乱麻，而是**一根方向向量**。

先给核心公式。Transformer 每层的输出都写进一条贯穿全网的"残差流"（residual stream），设某层某位置的激活为 h（一个几千维的向量），所谓 steering vector（转向向量）干预就是：

```text
h' = h + λ·v
```

v 是一根事先找到的方向向量（通常拿"有该行为的样本"和"无该行为的样本"的激活均值做差），λ 是强度系数。就这么一行加法，行为就变了。

这个家族不是一批平行的发现，是一条**时间上递进的线**，每一步都把胆子放大一圈：

**2024 年 6 月，开山之作：拒绝是一根轴。** refusal direction（拒绝方向，arXiv 2406.11717，NeurIPS 2024）：模型的"拒绝回答"行为由**一维子空间**中介——在 13 个开源模型（最大到 72B）上，把这一根方向从激活里减掉，模型就不再拒绝；加上去，无害问题也拒绝。（严谨起见：2026 年有工作 arXiv 2602.02132 提出反驳，认为某些设置下拒绝不止一根轴——"一根轴"是很好的一阶近似，别当铁律。）

**2024 年 10 月，工程化：指令也能变成轴。** instruction steering（arXiv 2410.12877，ICLR 2025，Microsoft）：把"请用 JSON 回答"这类**格式指令**提取成向量直接注入激活，不写进 prompt 也生效，而且可叠加（JSON + 小写同时注入）、可跨模型迁移。一根轴从"研究发现"变成了"工具箱"。

**2025 年 7 月，扩张到性格：人格也是轴。** persona vectors（人格向量，arXiv 2507.21509，Anthropic）：邪恶、谄媚、编造倾向，各自对应一根**可监控、可干预**的方向。我们之前也动手做过 persona steering 实验（ https://mp.weixin.qq.com/s/5EwUEXjFdD1ztjF8TWrvSA ），亲手拨过这根轴的人会对"人格是个向量"这句话有完全不同的体感。

注意这条线一路狂奔时埋下的一个**隐含假设**：这些工作全都默认，"找到能读出行为的方向"和"拿到能控制行为的方向"是同一件事——读出来的轴，拨它就灵。拒绝灵了，指令灵了，人格也灵了，于是没人怀疑这个假设本身。

直到 2026 年，有人回头查了这笔账。

━━━━━━━━━━━━━━━━━━━━

◆ 第四节：主菜——Perfect Detection, Failed Control

━━━━━━━━━━━━━━━━━━━━

来看这篇 2026 年 6 月的预印本（arXiv 2606.24952，注意：**预印本，未经同行评审**，结论强度自己打折）。它研究的格式行为特别接地气，就是每个用过小模型 JSON mode 的人都骂过的那个：**markdown 代码围栏**。

你要 JSON，模型给你的是：

````text
```json
{"answer": 42}
```
````

论文用的模型是 Gemma-2-2B-it（26 层，残差流 2304 维），默认情况下 100% 会给 JSON 套上这层围栏——`json.loads` 直接爆炸，你还得自己写正则剥壳。

不过先说清楚，省得你读原文时犯嘀咕：这篇论文的主角其实不是格式，是**幻觉**——作者研究的是"模型明明检测得到自己在编造，却控制不了"这条裂缝，格式在论文里只是个对照案例，用来证明"检测不等于控制"不是方法问题。而我们这篇文章的视角是反着借用的：**论文拿格式当陪衬讲幻觉的坏消息，我们拿幻觉当陪衬讲格式的好消息**。两个消息都值得听。先说坏消息——

**坏消息：幻觉看得见，推不动。** 作者给模型喂真假混合的实体（50 个真实 + 50 个编造的首都、科学名词之类），用线性探针读残差流，**从第 5 层起就能以 AUC = 1.000 完美分辨模型"知不知道这个实体是编的"**——检测这边，六种独立方法全部成功，这是 Perfect Detection。然后灾难开始了：沿着这根"看见幻觉"的方向去干预，想让模型拒绝编造——**数据驱动的方向推下去，编造反而变多**；换手工挑的方向，强度加到 α=15（相当于粗暴地拽），编造率才从约 70% 降到约 40%；SAE 特征放大 50 倍，无效；直接消融相关神经元，无效。这是 Failed Control。为什么推不动？作者算了一下："看见幻觉"的检测方向和"产生拒绝"的控制方向，cosine 相似度只有 0.12——**夹角 83°，接近正交**。你能看见它的窗户，和能推动它的门，根本不在一面墙上。

**好消息：格式不一样。** 同一套流程跑"套不套 markdown 围栏"这个行为，一切都反过来了：

- **决策位置**：用 logit lens（逐层透镜）——把每层的中间激活直接投影到词表、看"这一层想说什么"——观察 `{` 和 `` ``` `` 这两个 token 的倾向分岔（`` ``` `` 在词表里是单个 token，代码语料里它出现得太频繁了）。分岔发生在**第 20-25 层，峰值第 24 层**。前 20 层在算"答案是什么"，最后几层才拍板"用什么包装"。
- **方向的来历**（这个细节最妙）：围栏方向不需要跑模型收集激活，**直接从输出词表矩阵里减出来**——``d = normalize(W['{'] − W['```'])``，就是 `{` 和 `` ``` `` 两个 token 的 unembedding 向量之差。一行代码。
- **干预效果**：在第 20-25 层沿这根方向注入，强度 α=3.5——**折算下来仅占该层激活范数的 0.6%**——结果：围栏消除 100%，JSON 合法 100%，答案正确率 100%。而且是陡峭的开关型响应：α 低于 2.5 围栏一个不少，超过 3.5 一个不剩，中间几乎没有过渡带。

0.6% 是什么概念？在一个 2304 维的向量上加了一个几乎察觉不到的小扰动，模型的"内容脑区"完全没被打扰，只有"包装决策"被精准改写了。

把坏消息和好消息并排放在一起，才是这篇论文真正想说的话。同一个模型、同一套方法，两种行为给出了两种世界：

- **幻觉**：检测方向和控制方向夹角 83°，几乎正交——你能看见它，但从看见的那个角度使劲，推不动它。知与行分家。
- **格式**：检测它的轴就是控制它的轴，两个角色塌缩在同一根方向上——而且这根方向简单到可以直接从词表矩阵里减出来。

用一个词概括：**格式是模型内部"知行合一"的特例**。这也回答了上一节埋的那个问题——为什么"一根轴"家族一路顺风顺水：因为早期成员（拒绝、指令、格式）恰好都是知行合一的行为；而"说真话还是编造"缠绕在整个语义计算里，没有单独的开关可拨。**"找到方向 = 拿到控制权"不是定律，是运气**——这个领域花了两年才发现自己一直在特例里干活。

────────────────────

💡 顺便一提：怎么判断 0.12 算不算"接近正交"？

在 2304 维空间里，两根随机方向的 cosine 相似度期望约为 1/√2304 ≈ 0.02。所以 0.12 不是零——大约是随机水平的 6 倍，说明两根方向有微弱的关联；但离"同一根轴"（cosine 接近 1）差着十万八千里。高维空间里"几乎所有方向都互相正交"，这也是为什么"两根轴恰好重合"（格式）是需要解释的稀罕事，而"两根轴错开"（幻觉）才是默认状态。

────────────────────

到这儿，四块素材齐了：工程实现、格式税之争、一根轴家族、格式轴的直接证据。可以结账了。

━━━━━━━━━━━━━━━━━━━━

◆ 第五节：收束——格式和内容，在模型内部是分开放的

━━━━━━━━━━━━━━━━━━━━

把四节串起来，能得到一个挺干净的世界观：

**"说什么"是高维的，"用什么格式说"是低维的。** 答案内容需要模型动用几乎全部表示空间去计算；而格式——JSON 还是自然语言、套不套围栏——在内部被压缩成了少数几根轴，在网络的后段（Gemma-2 的第 20-25 层）才介入，像流水线末端的包装工序。这也解释了 instruction steering 为什么能把格式指令做成可叠加、可迁移的向量：低维的东西才好搬运。

于是"怎么让模型输出 JSON"其实有两条路线：

- **硬约束**：FSM 掐嘴，作用在**输出层**。优点是数学上保证合法；代价是扭曲分布，模型内部想走的路被外力硬拐。
- **软引导**：拨轴，作用在**思考层**（残差流）。优点是顺着模型自己的表示去改，内容不受干扰；代价是没有 100% 的合法性保证，而且你得先有权限摸到激活——API 用户暂时享受不到。

回到"格式税"之争，这套内部视角给出了一个还算公道的说法：**格式税不是必然的，约束加错了地方才收税。** 如果格式决策本来就是模型后段的一根轴，那么顺着这根轴走（好 prompt、好 schema、或者直接 steering），内容几乎无损——这是 dottxt 一方的道理；而 FSM 从第一个 token 就介入、且作用点和模型的格式表示完全错位，在 schema 和模型习惯冲突时就会把扭曲一路放大——这是 Let Me Speak Freely 一方观察到的现象。两边看到的都是真的，只是看的位置不同：一个看输出层的行为，一个（现在我们知道了）该去看第 24 层的那根轴。

━━━━━━━━━━━━━━━━━━━━

◆ 尾声：回到开篇的偏方

━━━━━━━━━━━━━━━━━━━━

**用 JSON 提问，有合理内核，但别神化。** 用本文的视角看：输入侧的 JSON 相当于给模型的"格式轴"一个免费的软信号——你没掐它的嘴、没动它的 logits，只是用输入的形状把它的内部状态往"结构化模式"推了一把，跟 persona 提示往残差流里画箭头是同一类操作。对**本身就该结构化的任务**（信息抽取、多字段填表、批量重复的流程化请求），这一把推得其名正言顺，效果好不奇怪；但对**需要自由推理和创意的任务**，你等于主动把模型摁向"填表模式"——"格式税"之争里正方测到的退化，你是在输入侧自愿交了。up 主视频里不会告诉你的是后半句。

和开头交代过的一样，这个说法是**我的推断，不是哪篇论文的结论**——"JSON 提问 vs 回答质量"这个具体问题，据我所知还没有人正经测过（谁看到相关实验记得告诉我）。四块拼图各自都是坚实的：掐嘴会扭曲分布是证明过的，格式税之争双方的实验都是真的，格式轴在第 24 层也是真的。但"输入侧 JSON = 给格式轴的软信号"这最后一跳，是我拼上去的。它是这些证据下最自然的解释，不代表它是对的。

下次你打开 JSON mode、或者往输入框里敲 JSON 的时候，可以想一下：合同（schema）是签在模型嘴上的，而它对格式的"想法"存放在倒数几层的一根方向向量里。这两个地方离得越近，你付的税就越少。

━━━━━━━━━━━━━━━━━━━━

【参考资料】

- Willard & Louf (2023). *Efficient Guided Generation for Large Language Models*. arXiv: 2307.09702（Outlines）
- Park et al. (2024). *Grammar-Aligned Decoding*. arXiv: 2405.21047（NeurIPS 2024）
- Tam et al. (2024). *Let Me Speak Freely? A Study on the Impact of Format Restrictions on Performance of Large Language Models*. arXiv: 2408.02442（EMNLP 2024 Industry）
- Will Kurt. *Say What You Mean: A Response to 'Let Me Speak Freely'*. blog.dottxt.co
- Geng et al. (2025). *JSONSchemaBench*. arXiv: 2501.10868
- Arditi et al. (2024). *Refusal in Language Models Is Mediated by a Single Direction*. arXiv: 2406.11717（NeurIPS 2024）
- arXiv: 2602.02132（2026，对"拒绝单方向"假设的反驳）
- Chen et al. (2025). *Persona Vectors*. arXiv: 2507.21509（Anthropic）
- Stolfo et al. (2024). *Improving Instruction-Following in Language Models through Activation Steering*. arXiv: 2410.12877（ICLR 2025, Microsoft）
- Galeone et al. (2026). *Perfect Detection, Failed Control: The Geometry of Knowing vs. Steering in Language Models*. arXiv: 2606.24952（预印本）

━━━━━━━━━━━━━━━━━━━━

「格式是模型内部"知行合一"的特例：读出它的轴，就是控制它的轴。」

「"说什么"是高维的，"用什么格式说"是低维的——低维的东西才好搬运。」

「格式税不是必然的，约束加错了地方才收税。」

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的严谨 × Gemini 的浪漫
// 2026-07-13
