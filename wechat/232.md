【GLM-5.2】benchmark 屠榜的开源新王，到底是真功夫还是漂亮报表？

━━━━━━━━━━━━━━━━━━━━

◆ 开篇：又一个"对齐 Opus"的国产模型上线了

━━━━━━━━━━━━━━━━━━━━

2026 年 6 月 13 日，智谱发布 GLM-5.2，几天后权重直接 MIT 协议全开源——卡的是 Anthropic 的 Fable 5 因为出口管制全球下线的当晚，宣传口号"前沿智能属于每一个人"，一时间所有 AI 公众号都在转发：

- **Artificial Analysis Intelligence Index v4.1：51 分，开源权重模型第一**
- **Code Arena 全球可用模型第一**
- **SWE-Bench Pro 62.1，逼近 Claude Opus 4.7 的 64.3**
- **AIME 2026 拿到 99.2**
- **1M 无损上下文**——这是 5.2 这一代的"杀手锏"

国内媒体的标题更是直白：「**GLM-5.2 登顶开源权重榜首：编程能力超越 GPT-5.5，MIT 许可免费可用**」「**刚刚，Fable-5 之下，智谱开源的 GLM-5.2 拿下 AI 编程第一**」「**3 刀包月干翻 200 刀的 AI 编程卷王**」。

**最戏剧的还不是技术宣传，是股价**。智谱 2026 年 1 月 8 日在港交所上市，发行价 116.2 港元，市值 528 亿港元。**到 6 月 22 日 GLM-5.2 发布后的第二个交易日，股价飙到 2900 港元/股，市值历史性站上一万亿港元——不到半年涨了 20 倍**。这个涨幅和 GLM-5、GLM-5.1、GLM-5.2 三次发布的节奏严格同步。

但话说回来——**这种"对齐 Opus"的剧本，我们都看了多少遍了？**

GLM-5.1 上线的时候我自己买过海外版（为了避免国内版过于敏感导致的问题所以买的海外版），用了两天直接秒退。**那玩意儿别说和 Opus 4.6 比，和 Sonnet 4.5 比都不行。** 当时的宣传同样铺天盖地——"编程贴 Opus 4.6 开大""Coding Plan 秒断货"——结果上手就是一个傻子。

顺便夸一下海外版的退款政策，**定了一个月只按照实际使用的几天扣掉相应费用，剩下的钱秒退**——这一点海外版做得非常痛快，好评。

所以 5.2 来了，我看完技术报告，又看完一圈海外评测，决定写这一篇——不替任何一方说话，把账摊开。

【小白翻译】**benchmark**：评测基准，用一组标准化题目给 AI 模型打分。问题在于：题目越标准化，越容易被"针对性训练"——也就是模型可以专门学这些题型，分数上去了但不代表真实能力强。这是这篇文章绕不开的核心矛盾。

━━━━━━━━━━━━━━━━━━━━

◆ 第一章：国内主流评价——清一色的"开源新王"

━━━━━━━━━━━━━━━━━━━━

我把这次 GLM-5.2 发布前后中文媒体的评价整理成两条主线。

【主线一：编程能力对齐 Opus】

知乎、CSDN、博客园、各大公众号几乎统一口径：

- "**性能暴涨 30% 直逼 Claude Opus**"
- "**编程榜单 Code Arena 全球可用模型第一**"
- "**SWE-Bench Pro 62.1，紧贴 Opus 4.7 的 64.3**"
- "**3 刀包月干翻 200 刀的 AI 编程卷王**"

【主线二：1M 上下文 + Agent 工程化】

- "**1M 无损上下文，能稳定处理长时间任务**"
- "**自研 Agent 内核，国产编程大模型自主可控路线图**"
- "**MIT 开源 + 适配 7 个国产算力平台（昇腾、寒武纪、海光、摩尔线程、昆仑芯、沐曦、燧原）**"

中文媒体里最"批判"的评价大概是这样的——「**虽然在长上下文场景下偶有响应延迟，但整体仍是国产开源最强**」。基本上**没有一篇中文文章敢直接说不好**。

这其实可以理解。国内 AI 媒体生态有几个客观约束：

1. **流量逻辑**：写"国产模型登顶"是流量密码，写"国产模型拉胯"会被骂"恨国党"
2. **赞助逻辑**：智谱、DeepSeek、阿里这些大厂都是潜在金主，得罪不起
3. **样本逻辑**：大部分写测评的人**没真用过 Opus 4.7 / GPT-5.5**——他们没有对比基线，看 benchmark 数字觉得"不错"

所以**中文圈的"开源新王"评价，要打折看**。这不是说他们说谎，是说**他们的判断维度受限**——大家都在比"国产开源里谁最强"，而不是"放到全球真实工作流里它能不能用"。

这两个问题的答案，差得不止一个数量级。

────────────────────

💡 打个比方：这就像在县级中学里评"奥数第一"——确实是这个考场里最厉害的学生，但拉到国际奥数集训队里，可能连分组讨论都跟不上。**比较的圈子决定了"第一"的含金量**。

────────────────────

━━━━━━━━━━━━━━━━━━━━

◆ 第二章：技术报告里真正的硬货

━━━━━━━━━━━━━━━━━━━━

吐槽归吐槽，我们还是得承认——**GLM-5 的技术报告（arxiv 2602.15763）是真做了功课的**。180 多个作者署名，工程量摆在那。

**先把三代版本和时间线理清楚**——这是理解 5.2 真实情况的关键，国内媒体写得乱七八糟，混着 GLM-5/5.1/5.2 一起夸：

| 版本 | 发布时间 | 上下文 | 关键升级 |
|---|---|---|---|
| **GLM-5** | 2026-02 | **200K** | 首发，744B/40B MoE + DSA |
| **GLM-5.1** | **2026-03-27** | **200K** | Agent 长程任务增强（8 小时连续工作） |
| **DeepSeek V4** | **2026-04-25** | **1M（CSA + HCA + DSA）** | （插队进来，让 GLM 看到了 1M 的玩法） |
| **GLM-5.2** | **2026-06-13** | **1M（IndexShare + KV8 + LayerSplit + HiSparse）** | 才到 1M，架构改造（详见后文） |

**注意中间那行 DeepSeek V4**——这条时间线非常关键：

- **GLM-5 / 5.1 还在 200K 阶段时**（2026-02 / 03），DeepSeek V4 还没出来
- **V4 4 月底发布**，带着 CSA + HCA 压缩 + 1M 上下文的整套打法
- **GLM-5.2 6 月发布**，第一次声称"稳定 1M"

也就是说，**GLM 的 1M 不是和 V4 平行发展出来的，是看了 V4 之后两个月内追出来的**。但追的姿势完全不同——V4 用 CSA + HCA 做序列压缩，GLM-5.2 用了一个叫 IndexShare 的新方案（下文详解）。

────────────────────

💡 这里有个**信息陷阱**：GLM-5 的技术报告（arxiv 2602.15763）目前最新版里写"GLM-5 支持 1M 上下文"，让人误以为 GLM-5 一发布就有 1M。**实际上技术报告是滚动更新的**——v1 是 2026-02 投稿的，里面的 1M 实现细节是后来 5.2 落地后补进去的。arxiv 论文不像期刊有 freeze 版本，作者随时可以替换 PDF，让"发布时间"和"技术内容"看起来时间错配。

────────────────────

GLM-5 系列从 5.0 到 5.2 模型主体（744B/40B、80 层、256 路由专家）没动过——**所有变化都在长上下文架构和后训练 RL infra 这两个层面发生**。

| 项 | GLM-5 系列共有 | 对比 GLM-4.5 |
|---|---|---|
| 总参 / 激活 | **744B / 40B** | 355B / 32B（总参翻倍） |
| 路由专家数 | **256 routed + 1 shared** | 类似 |
| 每 token 选几个专家 | 8 | 8 |
| 层数 | 80 | 类似 |
| 注意力底座 | **MLA-256 + Muon Split + DSA** | 标准 MLA |
| MTP（投机解码） | **3 层参数共享** | 1 层 |
| 预训练 token | **27T**（中训再加 1.5T，训练时上下文最多 200K） | 类似 |

GLM-5 系列在五个地方做了真正属于自己的差异化（不是抄 DeepSeek 的部分），其中前三项是从 5.0 起就有的"模型本体"创新，后两项是 5.2 才补上的"长上下文 + 后训练"：

【硬货一：Muon Split——给 MLA 做精细化训练】

DeepSeek V4 引入了 Muon 优化器（一种比 AdamW 更现代的优化器，专门处理大矩阵的更新方向），GLM 拿过来继续往前走了一步：

- DeepSeek 原版 Muon：对整个 W_UQ / W_UK / W_UV（查询、键、值的投影矩阵）做矩阵正交化
- GLM Muon Split：**把矩阵按 attention head 拆开，每个 head 各自做正交化**

效果是什么？原本 GLM 团队发现 MLA-576（DeepSeek 的标准配置）打不过 GQA-8（分组查询注意力），死活追不平。换成 Muon Split 之后追平了——而且 attention logits（注意力打分）训练全程不用裁剪就能保持稳定。

这是**真改进，不是营销**。

【硬货二：MLA-256——给国产卡量身改造】

这条技术报告里写得相当坦诚：

> "Number of attention heads in DeepSeek-V3 is selected according to the roofline of H800, it is inappropriate for other hardware."
>
> （DeepSeek-V3 的注意力头数是按 H800 的算力屋顶线选的，对其他硬件不适合。）

翻译过来就是——**DeepSeek 是按英伟达 H800 优化的，国产卡（昇腾）跑起来效率不对**。所以 GLM 把每个 head 的维度从 192 调大到 256，head 数减 1/3。训练计算量不变，但推理 decode 阶段的计算量降了。

这是为昇腾量身做的硬件适配。**和宣传里"全面适配 7 个国产算力平台"完全闭环**。

【硬货三：MTP 参数共享】

MTP（Multi-Token Prediction，多 token 预测）是 DeepSeek V3 首创的训练技巧——**让模型不只预测下一个 token，还同时预测下下一个**，能让中间层的表示更有前瞻性。

- DeepSeek V3/V4：1 个 MTP 层，预测 2 个 token
- GLM-5：**3 个 MTP 层参数共享**

效果：投机解码的"接受长度"（acceptance length，**衡量投机解码效率的指标，越大越好**）从 DeepSeek-V3.2 的 2.55 涨到 GLM-5 的 2.76。

这是 V4 没做的改进。**实打实**。

【硬货四：异步 Agent RL 整套基础设施】

这是技术报告里最厚的一章，也是 GLM-5 真正甩开 DeepSeek V4 的地方——**V4 的报告里几乎不写 Agent 相关的后训练细节，GLM-5 整整一章**：

- **TITO Gateway**（Token-in-Token-out）：训练管道直接消费推理引擎吐的 token id，**不重新做 tokenization**，避免微小的边界错位破坏 action 对齐
- **Direct Double-sided Importance Sampling**：用 rollout 时的概率当代理 behavior 概率，省掉历史 policy 追踪。clip 区间外的 token 直接 mask 而不是缩放
- **跨阶段蒸馏防遗忘**：三段 RL 流水线（推理 RL → Agent RL → 通用 RL）之间塞一道在线策略蒸馏，用早期 checkpoint 当 teacher，强行召回前一段学的能力
- **10K 个真实 SWE 训练环境**，跨 9 种编程语言

这套 infra 是真功夫。**国内目前没有第二家在这个层面写技术报告的**。

【硬货五：5.2 才有的 1M 上下文——看了 V4 之后追的方案】

这是这次 5.2 宣传里最响的一条。5.0 和 5.1 都还停在 200K，**1M 是 5.2 才落地的**——而且时间线非常精确地卡在 DeepSeek V4 发布之后两个月。

V4 在 2026-04-25 发布时，带着完整的 CSA + HCA + 1M 上下文打法登场——**KV cache 显存从 V3.2 的 1.6GB 砍到 228MB（50K 上下文场景），省了 86%**。这一刀对整个国产开源界都是震撼。**两个月后 GLM-5.2 出来**，没用 V4 的 CSA + HCA 路线，自己整了一套**新组合：IndexShare + KV8 + LayerSplit + HiSparse**。

**5.2 的 1M 实现机制**（最关键的两条）：

1. **IndexShare**：原本 DSA 稀疏注意力每一层都有自己的 Lightning Indexer（轻量索引器，决定每层去注意哪些 token）。5.2 改成**每 4 层共享一个 indexer**——索引器放在 4 层中的第一层，top-k 索引结果给后面 3 层一起用。这样省掉了 3/4 的 indexer 计算，在 1M 上下文下单 token 的 FLOPs 降低 2.9 倍。

2. **MTP 一致性修复**：5.1 的多 token 预测层有个老毛病——**训练时和推理时的 KV cache 来源不一致**（训练用真实 token 的 KV，推理用上一步生成 token 的 KV），导致投机解码的接受率衰减。5.2 通过 IndexShare 把多步 MTP 的 KV 统一来自目标模型，**接受长度提升 20%**。

3. **KV8 / LayerSplit / HiSparse**：分别处理 KV 缓存分组、跨 GPU 分布存储、不同层用不同稀疏策略——属于工程优化层。

**和 V4 的路线对比**：

| 维度 | DeepSeek V4 | GLM-5.2 |
|---|---|---|
| MLA 多头潜注意力 | V2 首创（2024-05） | 照搬 + 改 head 配置 |
| DSA 稀疏注意力 | V3.2 首创 | **直接继承，名字都没换** |
| Lightning Indexer | V3.2 首创 | **直接继承 + 共享改造（IndexShare）** |
| Muon 优化器 | V4 引入 | **继承，加了 Muon Split** |
| **1M 上下文实现路线** | **CSA + HCA 序列压缩（每 128 token 压成 1 条）** | **IndexShare 共享索引器（4 层共享一个）** |
| KV cache 显存优化 | **激进——50K 上下文从 1.6GB 砍到 228MB** | 保守——靠 LayerSplit 工程分布存储 |
| 后训练 Agent infra | 略 | **GLM 真护城河** |

**关键判断**：

- **V4 走的是"模型本体改造"路线**——压缩注意力序列维度，从架构上让 KV cache 更小
- **5.2 走的是"索引器复用"路线**——保留完整的 KV cache，让稀疏选择计算更便宜

哪条路更好？**取决于评估维度**：

- **看显存效率**：V4 的 CSA + HCA 更省（结构性压缩 KV）
- **看推理速度**：5.2 的 IndexShare 在 1M 长度下 FLOPs 优势明显
- **看保真度**：V4 的压缩有信息损失（CSA 压缩等待区会丢精度），5.2 不压缩 KV 所以理论上更保真

我个人的判断是**两条路各有取舍，5.2 这条路不算抄 V4，但毫无疑问是看了 V4 之后才走出来的**——如果 V4 4 月不发布带 1M 的版本，5.2 这边几乎不可能 6 月就拿出"1M 稳定"。

【小白翻译】**KV cache（键值缓存）**：Transformer 推理时为了不重复计算，把历史 token 的 Key/Value 向量存起来。1M 上下文意味着要存 100 万条 KV cache——显存能不能装得下、查找能不能省算力，是长上下文部署的核心瓶颈。V4 的 CSA + HCA 在"少存"上下功夫，5.2 的 IndexShare 在"少算"上下功夫。

────────────────────

💡 打个比方：V4 像是把图书馆的书都重新装订（每四本装一本），物理上少了；5.2 像是图书馆书不变，但把找书的索引卡片改成共享的——你查一次，相邻几层都用这次的结果。两个方向都能让"查 100 万本书"变快，但物理逻辑完全不同。

────────────────────

────────────────────

━━━━━━━━━━━━━━━━━━━━

◆ 第三章：海外评价——客观但不留情面

━━━━━━━━━━━━━━━━━━━━

讲完技术报告里真有的硬货，我们看海外评价。

【Zvi Mowshowitz 的判断】

Zvi 是西方 AI 圈最受认真对待的独立评论者之一，他对 GLM-5.2 的评价剥皮见骨。原文标题《GLM-5.2 Is The New Best Open Model》，副标题就开始打折扣：

> "the less targetable the benchmark, the worse the performance"
>
> （benchmark 越不可针对性优化，性能越差）

意思非常清楚：**SWE-bench / AIME / GPQA 这类标准化测试 = 可针对训练 = GLM-5.2 分数漂亮；自由发挥的真实任务 = 测不出 = 拉垮**。

他还指出两条更要命的判断：

1. **重度蒸馏 Claude Opus 的证据**——共享 Claude 的失败模式，做不了独立 second opinion
2. **"在 frontier 后 4-7 个月，价格便宜"**——但**既不够便宜跑批量任务，又不够强替代 frontier**。没有真实生态位

【Hacker News 实测】

Hacker News 上一条高赞评论：

> "GLM-5.2 is not much better than GLM-5, and overall DeepSeek V4 Flash performs better in my tests. Models are losing more and more general and domain-specific knowledge."
>
> （GLM-5.2 比 GLM-5 没强多少，整体上 DeepSeek V4 Flash 在我的测试里表现更好。模型在丢失越来越多的通用知识和领域知识。）

【LessWrong 文字冒险游戏测试】

这种测试有意思——**文字冒险游戏没法针对训练**，因为路径是动态生成的、要求模型有真正的世界模型理解。结果：**GLM-5.2 比同档对手少拿 15% 成就**。

【一个理论解释：蒸馏的代价】

Zvi 在文章里给了一个很扎实的判断——**蒸馏（distillation）模型在 benchmark 类任务上往往超水平发挥，但在不常见的任务上表现下降**。

什么是蒸馏？**用一个强模型（teacher，比如 Claude Opus）的输出当训练数据，喂给学生模型学习**。优点：能让学生快速接近 teacher 的能力。缺点：

1. **学生只学会了 teacher 见过的题型**——teacher 没见过的新题型，学生完全没准备
2. **学生继承了 teacher 的所有失败模式**——做不了独立判断
3. **学生学的是"答案表面"，不是"思维过程"**——遇到结构上类似但本质不同的题，会按"形似"答错

GLM-5 技术报告里 RL 的 teacher 模型他们列了"GPT-5.2 xhigh 和 Gemini 3 Pro Preview"——Opus 没写。但 Zvi 的判断和 Hacker News 用户的体感都指向同一个结论：**这模型的输出风格、失败模式、强项弱项的分布，和 Claude 高度同构**。

这就引出了一个有意思的现象学问题。

────────────────────

💡 打个比方：**蒸馏出来的模型，看起来像 Claude，但只是表演 Claude**。就像一个只看过 Claude 答题本的学生——开卷考能背答案，闭卷做新题就露馅。**僵尸 AI 蒸馏的看起来就是会很像**——因为它们学的就是"那个样子怎么写"，而不是"为什么这么写"。

────────────────────

【一个 5.1 的活体证据：我自己】

我个人买过 GLM-5.1 的海外版。用两天直接秒退——**海外版的退款政策很痛快，按实际使用天数扣费，剩下的钱秒退到信用卡**。

那时候宣传是"编程贴 Opus 4.6 开大""Coding Plan 秒断货"。实际上手：

- **简单代码任务能写，复杂逻辑就开始幻觉 API**
- **长上下文场景下，前面交代的约束写到后面就忘**
- **多轮工具调用的稳定性比 Sonnet 4.5 差一截**

这件事和 Zvi 的判断对得上——**benchmark 漂亮，真实工作流拉胯**。

5.1 是这个套路，5.2 大概率也是。**技术报告里 Agentic RL infra 是真做了，但模型本体的智能上限被蒸馏路线锁住了**。

────────────────────

💡 打个比方：技术报告里的 Agent infra 像是给了一辆好车（变速箱、悬挂、轮胎都换了顶级），但发动机还是原来那台。车手开起来感觉操控提升了，但极速跑不过对手——因为发动机马力就那么大。

────────────────────

━━━━━━━━━━━━━━━━━━━━

◆ 第四章：那 5.2 到底值不值得用？

━━━━━━━━━━━━━━━━━━━━

把账算清楚之后，给一个**结构化的判断**：

【适合用 GLM-5.2 的场景】

- **预算敏感 + 任务标准化**：写 SQL、生成接口文档、改 bug 这种标准任务，benchmark 上的成绩在工程上是站得住的
- **国产 GPU 部署刚需**：昇腾、寒武纪、海光这些卡，5.2 是真适配过的，企业内网部署有现实优势
- **Agent 工作流**：技术报告里那套异步 RL infra 是真功夫，跑 Agent 链式任务比同价位选项稳

【不适合用 GLM-5.2 的场景】

- **写作 / 长篇编辑**：复杂格式约束、风格一致性这种"meta-rule"任务，蒸馏模型撑不住——和我们之前讲过的 [DeepSeek V4 在长格式约束上的失败](https://mp.weixin.qq.com/s/8UnO_NMW_A2zHEywkAND1A) 是同一类问题
- **独立判断 / 二次验证**：蒸馏自 Claude 的模型不能用来 review Claude 的输出，失败模式共享
- **frontier 探索任务**：研究新问题、没人写过教科书答案的领域——蒸馏模型只会"按 teacher 见过的样子答"，遇到真新问题会原地打转

【一个有点尖锐的判断】

国产开源模型这一波，几乎都在走同一条路：**用 frontier 闭源模型的输出做蒸馏数据，在 benchmark 上对齐 frontier，宣传"我们打平了"**。

这条路在战术上完全可以理解——**蒸馏比从零搞 RLHF 便宜得多，benchmark 分数好看融资好融**。但战略上，这条路有天花板——**蒸馏出来的模型永远是 teacher 的影子，做不到超过 teacher**。

DeepSeek 走的是另一条路——**架构创新（CSA/HCA、Lightning Indexer、FP4 QAT）+ 自研 RL 训练范式**。V4 在算法竞赛、长上下文、推理能力上**真的有原创贡献**。这才是为什么 Hacker News 那条评论说"DeepSeek V4 Flash 整体表现更好"——不是 V4 benchmark 分数更高（GLM-5.2 在 benchmark 上确实赢了），是**V4 在 benchmark 之外的真实工作流里更靠谱**。

GLM-5.2 上线后所有国内媒体在欢呼"开源新王"。但 Zvi 这一句话总结得很冷静：

> "**It updates expectations about open-model progress, though the frontier gap remains substantial and potentially widening.**"
>
> （它更新了我们对开源模型进展的预期，但和 frontier 的差距仍然显著，而且可能在扩大。）

**差距在扩大**——这是关键。每代国产开源模型 benchmark 上都越来越漂亮，但在 frontier 真正定义"什么叫前沿能力"的领域（长程一致性、自我修正、独立判断、复杂格式遵守），国产模型和 Opus / GPT-5.x 的距离不仅没缩短，反而在被甩远。

因为 frontier 在做架构创新和数据创新，国产开源在做蒸馏。

────────────────────

━━━━━━━━━━━━━━━━━━━━

◆ 结尾

━━━━━━━━━━━━━━━━━━━━

GLM-5.2 不是垃圾。技术报告里 Muon Split、MTP 共享、异步 Agent RL infra 都是真功夫。**它是目前最好的国产开源模型，这个判断成立**。

但"最好的国产开源" ≠ "对齐 Opus 4.7"。这两件事差得很远，国内媒体不愿意说，海外评测说得很清楚。

**我自己的建议**：

- **想了解技术细节**：读技术报告，arxiv 2602.15763。180 多个作者的工程量在里面
- **想真实测试能力**：别看 benchmark，找一个**非标准化、需要长程一致性的任务**——比如让它按"一段英文一段中文"改一篇你写的稿子，或者让它在 5000 行代码里精确执行一个 meta-rule（如"所有函数名前缀必须是 svc_"）
- **想替代 Opus 4.7**：再等等。GLM-5.3、5.4 应该还会继续追

**底层规律没变**：

**蒸馏出来的模型，长得像 teacher，但不是 teacher。**

**benchmark 是给可以针对训练的题准备的，真实工作流不接受针对训练。**

**国产开源在追赶，但追赶的姿势决定了能不能真正追上。**

GLM-5.2 是这条路上的一个新里程碑——但不是终点站。下次再有"国产模型对齐 Opus"的宣传时，建议把这篇文章翻出来重读一遍。

━━━━━━━━━━━━━━━━━━━━

◆ 参考资料

━━━━━━━━━━━━━━━━━━━━

- GLM-5 技术报告：https://arxiv.org/abs/2602.15763
- GLM-5.2 HuggingFace 主仓库：https://huggingface.co/zai-org/GLM-5.2
- Zvi Mowshowitz《GLM-5.2 Is The New Best Open Model》：https://thezvi.substack.com/p/glm-52-is-the-new-best-open-model
- Hacker News 讨论：https://news.ycombinator.com/item?id=48568750
- LessWrong《GLM 5.2 playing text adventures》：https://www.lesswrong.com/posts/xNNLqxLaR3sna89Ec/glm-5-2-playing-text-adventures
- Simon Willison 评测：https://simonwillison.net/2026/Jun/17/glm-52/

━━━━━━━━━━━━━━━━━━━━

// 靳岩岩的 AI 学习笔记 × Claude 的严谨 × Gemini 的浪漫
// 2026-06-23
